{{!--
  Halaman: Create Value — Super Admin
  Lokasi: src/views/super_admin/value/create.hbs
  Level: L4 Page

  Route: GET /value/formCreate -> POST /value (urlencoded).
  Field dikirim sebagai title[id|en|ja], icon, description[id|en|ja];
  `valueOrder` tidak ikut — service yang menentukannya (max + 1 saat create,
  di-reindex saat delete).

  Toast sukses tidak dirender di sini: submit redirect ke /value, jadi
  notifikasinya muncul di index.hbs lewat flash `toast`.
--}}
<div class='min-h-screen bg-[#f3f3f3] px-4 py-8 sm:px-[34px] sm:py-[42px]'>
  <div class='flex flex-col gap-7'>

    <div class='flex flex-col gap-1'>
      {{> components/ui/super_admin/page_header/index
        title='Create Value'
      }}

      {{> components/ui/super_admin/description/index
        text='Create a new value for use across Kesatria Academy.'
      }}
    </div>

    <form
      action='/value'
      method='post'
      novalidate
      x-data='iconItemForm()'
      @submit.prevent='submitForm($event)'
    >
      {{#> components/ui/super_admin/form/icon_item/fields
        titleHint='Shown as the heading of the value card on the public About page.'
        descriptionHint='Shown under the heading on the value card.'
      }}
        <div class='flex flex-col-reverse gap-3 sm:flex-row sm:justify-end'>
          {{> components/ui/super_admin/form/button
            as='link'
            variant='secondary'
            href='/value'
            label='Cancel'
            loading=true
          }}

          {{> components/ui/super_admin/form/button
            type='submit'
            variant='primary'
            label='Create Value'
            loading=true
          }}
        </div>
      {{/components/ui/super_admin/form/icon_item/fields}}
    </form>
  </div>
</div>

{{> components/ui/super_admin/form/icon_item/form }}

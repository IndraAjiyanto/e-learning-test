<div
  class='pt-28 py-20'
  x-data='{ bulan: {{cicilan.bulan}}, hargaInputs: {{json cicilan.harga}} }'
>
  <div class='max-w-2xl mx-auto px-6 py-10 bg-white rounded-xl shadow-lg'>
    <h2 class='text-2xl font-semibold mb-6 text-gray-900'>Edit Cicilan</h2>

    <form
      action='/cicilan/{{cicilan.id}}?_method=PATCH'
      method='POST'
      class='space-y-6'
    >

      <!-- Pilih Bulan -->
      <div>
        <label
          for='bulan'
          class='block text-sm font-semibold text-gray-700 mb-1'
        >
          Installment Period
        </label>
        <select
          id='bulan'
          name='bulan'
          x-model='bulan'
          @change="hargaInputs = Array(parseInt(bulan)).fill('')"
          required
          class='w-full border bg-white border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
        >
          <option value='3'>3 Months</option>
          <option value='6'>6 Months</option>
          <option value='12'>12 Months</option>
        </select>
      </div>

      <!-- Input Harga Per Bulan -->
      <div>
        <label class='block text-sm font-semibold text-gray-700 mb-1'>
          Monthly Payment Amounts
        </label>
        <!-- Down Payment (DP) -->
        <div class='mb-4'>
          <label
            for='dp'
            class='block text-sm font-semibold text-gray-700 mb-1'
          >
            Down Payment (DP)
          </label>
          <input
            type='number'
            id='dp'
            name='dp'
            placeholder='Enter down payment amount'
            required
            min='0'
            value='{{cicilan.dp}}'
            class='w-full border bg-white border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
          />
        </div>
        <div class='space-y-2'>
          <template x-for='(price, index) in parseInt(bulan)' :key='index'>
            <div class='flex items-center gap-3'>
              <span
                class='text-sm text-gray-600 w-20'
                x-text="'Month ' + (index + 1)"
              ></span>
              <input
                type='number'
                :name="'harga[' + index + ']'"
                x-model='hargaInputs[index]'
                placeholder='Enter amount'
                required
                min='0'
                class='flex-1 border bg-white border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
              />
            </div>
          </template>
        </div>
      </div>

      <!-- Buttons -->
      <div class='flex gap-3'>
        <button
          type='submit'
          class='bg-gray-900 text-white px-6 py-2 rounded-lg shadow hover:bg-gray-600 transition'
        >
          Update
        </button>
        <a
          href='/kelass/{{cicilan.kelas.id}}'
          class='px-6 py-2 rounded-lg border border-gray-300 text-gray-700 hover:bg-gray-100 transition'
        >
          Cancel
        </a>
      </div>
    </form>
  </div>
</div>
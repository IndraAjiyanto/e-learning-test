{{!--
  Halaman: Create Alumni (per Program) — Super Admin
  Lokasi: src/views/super_admin/alumni/create.hbs
  Level: L4 Page

  Route: GET /alumni/formCreate/:courseId -> POST /alumni/:courseId
  (multipart: profile + name/message/currentPosition per bahasa).
  Data: { courseId }.

  courseId diambil controller dari URL, jadi field Program disembunyikan.
--}}
<div class='min-h-screen bg-[#f3f3f3] px-4 py-8 sm:px-[34px] sm:py-[42px]'>
  <div class='flex flex-col gap-7'>
    <div class='flex flex-col gap-1'>
      {{> components/ui/super_admin/page_header/index
        title='Create Alumni'
      }}

      {{> components/ui/super_admin/description/index
        text='Create a new alumni profile for Kesatria Academy.'
      }}
    </div>

    <form
      action='/alumni/{{courseId}}'
      method='post'
      enctype='multipart/form-data'
      novalidate
      x-data='alumniForm()'
      @submit.prevent='submitForm($event)'
      class='rounded-xl border border-[#d9d9d9] bg-white p-4 sm:p-5'
    >
      <div class='flex flex-col gap-5'>
        {{> components/ui/super_admin/form/alumni/fields hideProgram=true }}

        <div class='flex flex-col-reverse gap-3 pt-6 sm:flex-row sm:justify-end'>
          {{> components/ui/super_admin/form/button
            as='link'
            variant='secondary'
            href=(concat '/program/detail/program/admin/' courseId)
            label='Cancel'
            loading=true
          }}

          {{> components/ui/super_admin/form/button
            type='submit'
            variant='primary'
            label='Create Alumni'
            loading=true
          }}
        </div>
      </div>
    </form>
  </div>
</div>

{{> components/ui/super_admin/form/alumni/form }}

{{!--
  Halaman: Pengelolaan Background — Super Admin
  Lokasi: src/views/super_admin/background/index.hbs
  Level: L4 Page

  - Shell kartu + pagination dari table/card
  - Header kolom dari table/header_cell
  - Tombol aksi dari table/row_actions terintegrasi dengan modal delete_confirm

  `lang` hidup di scope Alpine bersarang: getter `rows` dan `totalPages`
  milik clientTable() akan hilang bila state digabung.
--}}
<div class='min-h-screen bg-[#f3f3f3] px-4 py-8 sm:px-[34px] sm:py-[42px]'>
  <div class='flex flex-col gap-7'>

    <div class='flex flex-col gap-1'>
      {{> components/ui/super_admin/page_header/index
        title='Background'
      }}

      {{> components/ui/super_admin/description/index
        text='Manage educational background and academic milestones displayed across Kesatria Academy.'
      }}
    </div>

    <div
      class='flex flex-col gap-3.5'
      x-data='clientTable({ items: {{json background}}, searchKeys: ["content.id", "content.en", "content.ja", "details.id", "details.en", "details.ja"] })'
    >
      {{> components/ui/super_admin/table/toolbar
        placeholder='Search background...'
        searchLabel='Search background'
        createHref='/background/formCreate'
        createLabel='Create Background'
      }}

      <div
        class='rounded-xl border border-[#d9d9d9] bg-white px-5 pb-5 pt-3'
        x-data="{ lang: 'id' }"
      >
        <div class='flex flex-col gap-5'>

          {{> components/ui/super_admin/tabs/language/index ariaLabel='Background language' }}

          {{#> components/ui/super_admin/table/card tableClass='min-w-[1010px]'}}
            <colgroup>
              <col class='w-[66px]' />
              <col class='w-[340px]' />
              <col />
              <col class='w-[116px]' />
            </colgroup>
            <thead>
              <tr>
                {{> components/ui/super_admin/table/header_cell label='No' }}
                {{> components/ui/super_admin/table/header_cell label='Content' }}
                {{> components/ui/super_admin/table/header_cell label='Text' }}
                {{> components/ui/super_admin/table/header_cell label='Action' }}
              </tr>
            </thead>
            <tbody>
              <template x-for='(row, index) in rows' :key='row.id'>
                <tr class='transition-colors hover:bg-[#fafafa]'>

                  {{> components/ui/super_admin/table/cell
                    text='String((currentPage - 1) * size + index + 1).padStart(2, "0")'
                  }}

                  {{#> components/ui/super_admin/table/cell}}
                    <span
                      class='line-clamp-2 font-inter text-sm font-medium leading-5 text-[#171717]'
                      x-text='(row.content && row.content[lang]) || "—"'
                    ></span>
                  {{/components/ui/super_admin/table/cell}}

                  {{#> components/ui/super_admin/table/cell}}
                    <span
                      class='line-clamp-2 font-inter text-sm leading-5 text-[#525252]'
                      x-text='(row.details && row.details[lang]) || "—"'
                    ></span>
                  {{/components/ui/super_admin/table/cell}}

                  {{#> components/ui/super_admin/table/cell variant='action'}}
                    {{> components/ui/super_admin/table/row_actions
                      entity='background'
                      deleteAction="'/background/' + row.id + '?_method=DELETE'"
                      editHref="'/background/formEdit/' + row.id"
                    }}
                  {{/components/ui/super_admin/table/cell}}
                </tr>
              </template>

              {{#> components/ui/super_admin/table/message_row show='rows.length === 0' colspan='4'}}
                No background found.
              {{/components/ui/super_admin/table/message_row}}
            </tbody>
          {{/components/ui/super_admin/table/card}}
        </div>
      </div>
    </div>
  </div>
</div>

{{> components/ui/super_admin/modal/delete_confirm/index
  title='Delete Background?'
  description='Are you sure you want to delete this background? This action will permanently remove it and cannot be undone.'
}}

{{> components/ui/super_admin/toast/success/index toast=toast }}

{{> components/ui/super_admin/table/client_table }}

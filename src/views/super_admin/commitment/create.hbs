{{!--
  Halaman: Create Commitment — Super Admin
  Lokasi: src/views/super_admin/commitment/create.hbs
  Level: L4 Page

  Route: GET /commitment/formCreate -> POST /commitment (urlencoded).
  Field dikirim sebagai title[id|en|ja], icon, description[id|en|ja];
  `commitmentOrder` tidak ikut — service yang menentukannya (max + 1 saat create,
  di-reindex saat delete).

  Toast sukses tidak dirender di sini: submit redirect ke /commitment, jadi
  notifikasinya muncul di index.hbs lewat flash `toast`.
--}}
<div class='min-h-screen bg-[#f3f3f3] px-4 py-8 sm:px-[34px] sm:py-[42px]'>
  <div class='flex flex-col gap-7'>

    <div class='flex flex-col gap-1'>
      {{> components/ui/super_admin/page_header/index
        title='Create Commitment'
      }}

      {{> components/ui/super_admin/description/index
        text='Create a new commitment for use across Kesatria Academy.'
      }}
    </div>

    <form
      action='/commitment'
      method='post'
      novalidate
      x-data='iconItemForm()'
      @submit.prevent='submitForm($event)'
    >
      {{#> components/ui/super_admin/form/icon_item/fields
        titleHint='Shown as the heading of the commitment card on the public About page.'
        descriptionHint='Shown under the heading on the commitment card.'
      }}
        <div class='flex flex-col-reverse gap-3 sm:flex-row sm:justify-end'>
          {{> components/ui/super_admin/form/button
            as='link'
            variant='secondary'
            href='/commitment'
            label='Cancel'
            loading=true
          }}

          {{> components/ui/super_admin/form/button
            type='submit'
            variant='primary'
            label='Create Commitment'
            loading=true
            disabled='!canSubmit() || loading'
          }}
        </div>
      {{/components/ui/super_admin/form/icon_item/fields}}
    </form>
  </div>
</div>

{{> components/ui/super_admin/form/icon_item/form }}

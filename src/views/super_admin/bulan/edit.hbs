<div
  class='py-10'
  x-data="{
  // Form validation states
  formData: {
    bulan: '{{bulan.bulan}}'
  },
  formErrors: {
    bulan: ''
  },
  formValid: {
    bulan: false
  },

  validateField(field, value) {
    this.formData[field] = value;
    
    switch(field) {
      case 'bulan':
        const numValue = parseInt(value);
        if (!value || isNaN(numValue)) {
          this.formErrors.bulan = 'Month number is required';
          this.formValid.bulan = false;
        } else if (numValue < 1 || numValue > 12) {
          this.formErrors.bulan = 'Month must be between 1 and 12';
          this.formValid.bulan = false;
        } else {
          this.formErrors.bulan = '';
          this.formValid.bulan = true;
        }
        break;
    }
  },

  canSubmit() {
    return this.formValid.bulan;
  },

  hasFormErrors() {
    return Object.values(this.formErrors).some(error => error !== '');
  }
}"
>
  <div class='max-w-2xl mx-auto px-6 py-10 bg-white rounded-xl shadow-lg'>
    <h2 class='text-2xl font-semibold mb-6 text-gray-900'>Edit Bulan</h2>
    <!-- Removed 'Back to list' link to match pertanyaan_umum pattern -->

    <form
      action='/bulan/{{bulan.id}}?_method=PATCH'
      method='post'
      @submit='if (!canSubmit()) { $event.preventDefault(); return false; }'
      class='space-y-6'
    >

      <div>
        <label
          for='bulan'
          class='block text-sm font-semibold text-gray-700 mb-1'
        >Month (number)</label>
        <input
          x-model='formData.bulan'
          @input='validateField("bulan", $event.target.value)'
          id='bulan'
          name='bulan'
          type='number'
          min='1'
          max='12'
          required
          value='{{bulan.bulan}}'
          :class="formErrors.bulan ? 'border-red-500 focus:ring-red-500 focus:border-red-500' : formValid.bulan ? 'border-green-500 focus:ring-green-500 focus:border-green-500' : 'border-gray-300 focus:ring-gray-900'"
          class='w-full border bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 text-gray-700'
        />
        <div
          x-show='formErrors.bulan'
          class='mt-1 text-sm text-red-600'
          x-text='formErrors.bulan'
        ></div>
        <div
          x-show='formValid.bulan && !formErrors.bulan'
          class='mt-1 text-sm text-green-600'
        >✓ Month number looks good</div>
      </div>

      <div class='flex gap-3'>
        <button
          type='submit'
          :disabled='!canSubmit()'
          :class="canSubmit() ? 'bg-gray-900 hover:bg-gray-600' : 'bg-gray-400 cursor-not-allowed'"
          class='text-white px-6 py-2 rounded-lg shadow transition'
        >Update</button>
        <a
          href='/bulan'
          class='px-6 py-2 rounded-lg border border-gray-300 text-gray-700 hover:bg-gray-100 transition'
        >Cancel</a>
      </div>
    </form>
  </div>
</div>
<div class="max-w-7xl mx-auto px-6 py-10" <div class='container mx-auto px-4 py-8'>

     x-data="teamLeadTable({ teamLeads: {{json teamLeads}} })">  <div class='flex justify-between items-center mb-6'>

  <div class="mb-6 flex flex-col md:flex-row md:justify-between md:items-center gap-4">    <h1 class='text-3xl font-bold text-gray-800'>Team Lead Management</h1>

    <h2 class="text-3xl font-bold text-gray-900 text-center md:text-left">Team Lead</h2>    <a

      href='/team-lead/formCreate'

    <div class="flex items-center gap-3">      class='bg-gray-900 hover:bg-gray-700 text-white font-semibold py-2 px-6 rounded-lg shadow-lg transition duration-300 ease-in-out transform hover:scale-105'

      <div class="relative w-full md:w-64">    >

        <input       <i class='fas fa-plus mr-2'></i>Add Team Lead

          type="text"     </a>

          placeholder="Search team lead..."   </div>

          x-model="searchInput"

          @input="search = searchInput; pageNumber=0"  {{#if teamLeads}}

          class="border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 pr-10 focus:outline-none focus:ring-2 focus:ring-gray-900 w-full"    <div class='bg-white shadow-lg rounded-lg overflow-hidden'>

        />      <div class='overflow-x-auto'>

        <button         <table class='min-w-full divide-y divide-gray-200'>

          @click="search = searchInput; pageNumber=0"          <thead class='bg-gray-900'>

          class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-gray-900"            <tr>

        >              <th

          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 1110.5 3a7.5 7.5 0 016.15 13.65z"/>              >

          </svg>                Profile

        </button>              </th>

      </div>              <th

                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

      <a href="/team-lead/formCreate"               >

         class="bg-gray-900 text-white px-4 py-2 rounded-lg shadow hover:bg-gray-600 transition whitespace-nowrap">                Name

        Create Team Lead              </th>

      </a>              <th

    </div>                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

  </div>              >

                Position

<div class="overflow-x-auto bg-white shadow-lg rounded-xl">              </th>

  <table class="min-w-full divide-y divide-gray-200">              <th

    <thead class="bg-gray-100">                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

      <tr>              >

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">No</th>                Email

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Profile</th>              </th>

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Name</th>              <th

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Position</th>                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Email</th>              >

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Phone</th>                Phone

        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Action</th>              </th>

      </tr>              <th

    </thead>                class='px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider'

    <tbody class="divide-y divide-gray-200">              >

      {{#each teamLeads}}                LinkedIn

      <tr class="hover:bg-gray-50 transition"              </th>

          x-show="filteredTeamLeads.includes('{{id}}')">              <th

        <td class="px-6 py-4 text-sm text-gray-800" x-text="filteredTeamLeads.indexOf('{{id}}') + 1 + (pageNumber * size)"></td>                class='px-6 py-4 text-center text-xs font-bold text-white uppercase tracking-wider'

        <td class="px-6 py-4 text-sm text-gray-800">              >

          <img src="{{profile}}"                 Actions

               alt="{{nama}}"               </th>

               class="w-12 h-12 rounded-full object-cover border" />            </tr>

        </td>          </thead>

        <td class="px-6 py-4 text-sm text-gray-800">{{nama}}</td>          <tbody class='bg-white divide-y divide-gray-200'>

        <td class="px-6 py-4 text-sm text-gray-800">{{posisi}}</td>            {{#each teamLeads}}

        <td class="px-6 py-4 text-sm text-gray-800">{{email}}</td>              <tr class='hover:bg-gray-50 transition-colors duration-200'>

        <td class="px-6 py-4 text-sm text-gray-800">{{phone}}</td>                <td class='px-6 py-4 whitespace-nowrap'>

<td class="px-6 py-4 text-sm text-gray-800 flex items-center gap-3">                  <img

  <!-- Edit Button -->                    src='{{profile}}'

  <form action="/team-lead/formEdit/{{id}}" method="get">                    alt='{{nama}}'

    <button type="submit"                     class='h-12 w-12 rounded-full object-cover border-2 border-gray-300'

            class="p-2 rounded-lg bg-blue-50 text-blue-600 hover:bg-blue-100 hover:text-blue-800 transition"                   />

            title="Edit">                </td>

      <!-- Pencil Icon -->                <td class='px-6 py-4 whitespace-nowrap'>

      <svg xmlns="http://www.w3.org/2000/svg"                   <div class='text-sm font-medium text-gray-900'>{{nama}}</div>

           fill="none" viewBox="0 0 24 24"                 </td>

           stroke-width="1.5" stroke="currentColor"                 <td class='px-6 py-4 whitespace-nowrap'>

           class="w-5 h-5">                  <div class='text-sm text-gray-700'>{{posisi}}</div>

        <path stroke-linecap="round" stroke-linejoin="round"                 </td>

              d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652                 <td class='px-6 py-4 whitespace-nowrap'>

                 2.652L10.582 16.07a4.5 4.5 0 01-1.897                   <div class='text-sm text-gray-700'>{{email}}</div>

                 1.13l-2.685.805.805-2.685a4.5 4.5                 </td>

                 0 011.13-1.897l10.927-10.936z" />                <td class='px-6 py-4 whitespace-nowrap'>

        <path stroke-linecap="round" stroke-linejoin="round"                   <div class='text-sm text-gray-700'>{{phone}}</div>

              d="M19.5 7.125L16.875 4.5" />                </td>

      </svg>                <td class='px-6 py-4 whitespace-nowrap'>

    </button>                  <a

  </form>                    href='{{linkedin}}'

                    target='_blank'

  <!-- Delete Button -->                    class='text-blue-600 hover:text-blue-800 text-sm'

  <form action="/team-lead/{{id}}?_method=DELETE" method="post">                  >

    <button type="button"                     <i class='fab fa-linkedin mr-1'></i>LinkedIn

            onclick="confirmDelete(this)"                   </a>

            class="p-2 rounded-lg bg-red-50 text-red-600 hover:bg-red-100 hover:text-red-800 transition"                 </td>

            title="Delete">                <td class='px-6 py-4 whitespace-nowrap text-center'>

      <!-- Trash Icon -->                  <div class='flex justify-center space-x-2'>

      <svg xmlns="http://www.w3.org/2000/svg"                     <a

           fill="none" viewBox="0 0 24 24"                       href='/team-lead/formEdit/{{id}}'

           stroke-width="1.5" stroke="currentColor"                       class='bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-lg transition duration-300 ease-in-out transform hover:scale-105'

           class="w-5 h-5">                    >

        <path stroke-linecap="round" stroke-linejoin="round"                       <i class='fas fa-edit'></i>

              d="M6 18L18 6M6 6l12 12" />                    </a>

      </svg>                    <form

    </button>                      action='/team-lead/{{id}}?_method=DELETE'

  </form>                      method='POST'

</td>                      class='inline-block'

                      onsubmit="return confirm('Are you sure you want to delete this team lead?');"

      </tr>                    >

      {{/each}}                      <button

    </tbody>                        type='submit'

  </table>                        class='bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded-lg transition duration-300 ease-in-out transform hover:scale-105'

</div>                      >

                        <i class='fas fa-trash'></i>

                      </button>

  <div class="w-full md:w-1/2 mx-auto py-6 flex justify-center gap-2 items-center"                     </form>

       x-show="pageCount() > 1">                  </div>

    <button @click="prevPage" :disabled="pageNumber==0"                </td>

      class="px-3 py-1 rounded border border-gray-900 text-gray-900"              </tr>

      :class="{ 'opacity-50 cursor-not-allowed': pageNumber==0 }">Prev</button>            {{/each}}

          </tbody>

    <template x-for="(page,index) in pages()" :key="index">        </table>

      <button @click="viewPage(index)"       </div>

        class="px-3 py-1 rounded"    </div>

        :class="{ 'bg-gray-900 text-white font-bold': index===pageNumber }"  {{else}}

        x-text="index+1"></button>    <div class='bg-white shadow-lg rounded-lg p-12 text-center'>

    </template>      <div class='mb-4'>

        <i class='fas fa-users text-6xl text-gray-300'></i>

    <button @click="nextPage" :disabled="pageNumber >= pageCount()-1"      </div>

      class="px-3 py-1 rounded border border-gray-900 text-gray-900"      <h3 class='text-xl font-semibold text-gray-700 mb-2'>No Team Leads Yet</h3>

      :class="{ 'opacity-50 cursor-not-allowed': pageNumber >= pageCount()-1 }">Next</button>      <p class='text-gray-500 mb-6'>Start by adding your first team lead member.</p>

  </div>      <a

</div>        href='/team-lead/formCreate'

        class='inline-block bg-gray-900 hover:bg-gray-700 text-white font-semibold py-2 px-6 rounded-lg shadow-lg transition duration-300 ease-in-out transform hover:scale-105'

<script>      >

function teamLeadTable({ teamLeads }) {        <i class='fas fa-plus mr-2'></i>Add Team Lead

  return {      </a>

    searchInput: "",    </div>

    search: "",  {{/if}}

    pageNumber: 0,</div>
    size: 5,
    teamLeads,
    amount: 0,
    get filteredTeamLeads() {
      let filtered = this.teamLeads.filter(t =>
        (`${t.nama} ${t.posisi} ${t.email} ${t.phone}`).toLowerCase()
          .includes(this.search.toLowerCase())
      );

      this.amount = filtered.length;

      const start = this.pageNumber * this.size;
      const end = start + this.size;
      return filtered.slice(start, end).map(t => t.id.toString());
    },
    pages() { return Array.from({ length: this.pageCount() }); },
    pageCount() { return Math.ceil(this.amount / this.size); },
    nextPage() { if (this.pageNumber < this.pageCount()-1) this.pageNumber++; },
    prevPage() { if (this.pageNumber > 0) this.pageNumber--; },
    viewPage(index) { this.pageNumber = index; },
  }
}
</script>

<div class="py-10">
  <div class="max-w-4xl mx-auto px-6 py-10 bg-white rounded-xl shadow-lg">
    <h2 class="text-2xl font-semibold mb-6 text-gray-900">Edit Team Lead</h2>

    <form action="/team-lead/{{teamLead.id}}?_method=PATCH" method="POST" enctype="multipart/form-data" class="space-y-6">

      <div 
        x-data="{ 
          previewUrl: null,
          oldPhoto: '{{#if teamLead.profile}}{{teamLead.profile}}{{else}}/images/default-avatar.png{{/if}}'
        }" 
        class="space-y-3"
      >
        <label for="profile" class="block text-sm font-semibold text-gray-700 mb-1">
          Profile Photo
        </label>

        <input 
          type="file" 
          id="profile" 
          name="profile" 
          accept="image/*"
          x-ref="fileInput"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700"
          @change="previewUrl = $event.target.files[0] ? URL.createObjectURL($event.target.files[0]) : null"
        >

        <div class="relative w-32 h-32">
          <img 
            :src="previewUrl ? previewUrl : oldPhoto" 
            alt="Preview Photo" 
            class="w-32 h-32 object-cover rounded-lg border border-gray-300"
          >

          <template x-if="previewUrl">
            <button 
              type="button" 
              class="absolute -top-2 -right-2 bg-red-500 text-white w-6 h-6 rounded-full flex items-center justify-center shadow hover:bg-red-600"
              @click="previewUrl = null; $refs.fileInput.value = null;">
              &times;
            </button>
          </template>
        </div>
      </div>

      <div>
        <label for="nama" class="block text-sm font-semibold text-gray-700 mb-1">Name</label>
        <input 
          type="text" 
          id="nama" 
          name="nama" 
          value="{{teamLead.nama}}" 
          required
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >
      </div>

      <div>
        <label for="posisi" class="block text-sm font-semibold text-gray-700 mb-1">Position</label>
        <input 
          type="text" 
          id="posisi" 
          name="posisi" 
          value="{{teamLead.posisi}}" 
          required
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >
      </div>

      <div>
        <label for="deskripsi" class="block text-sm font-semibold text-gray-700 mb-1">Description</label>
        <textarea 
          id="deskripsi" 
          name="deskripsi" 
          required
          rows="3"
          placeholder="Short description about the team lead..."
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >{{teamLead.deskripsi}}</textarea>
      </div>

      <div>
        <label for="twitter" class="block text-sm font-semibold text-gray-700 mb-1">Twitter URL</label>
        <input 
          type="url" 
          id="twitter" 
          name="twitter" 
          value="{{teamLead.twitter}}" 
          placeholder="https://twitter.com/username"
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >
      </div>

      <div>
        <label for="github" class="block text-sm font-semibold text-gray-700 mb-1">GitHub URL</label>
        <input 
          type="url" 
          id="github" 
          name="github" 
          value="{{teamLead.github}}" 
          placeholder="https://github.com/username"
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >
      </div>

      <div>
        <label for="linkedin" class="block text-sm font-semibold text-gray-700 mb-1">LinkedIn URL</label>
        <input 
          type="url" 
          id="linkedin" 
          name="linkedin" 
          value="{{teamLead.linkedin}}" 
          placeholder="https://linkedin.com/in/username"
          class="w-full border border-gray-300 bg-white text-gray-900 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900"
        >
      </div>

      <div class="flex space-x-4">
        <button type="submit"
          class="bg-gray-900 text-white px-6 py-2 rounded-lg shadow hover:bg-gray-600 transition focus:outline-none focus:ring-2 focus:ring-gray-900">
          Update
        </button>
        <a href="/team-lead"
          class="bg-white border border-gray-300 text-gray-900 px-6 py-2 rounded-lg shadow hover:bg-gray-100 transition focus:outline-none focus:ring-2 focus:ring-gray-900">
          Cancel
        </a>
      </div>
    </form>
  </div>
</div>



<div class="w-full flex justify-center items-center">
  <h2 class="relative text-4xl md:text-5xl font-bold leading-tight font-montserrat {{textColor}}">
    
    {{before}}

    <span class="relative z-10 px-0">
      {{highlight}}

      <!-- half background -->
      <span 
        class="absolute left-0 right-0 bottom-0 {{bg}} h-1/2 w-full -z-10"
      ></span>

    </span>

    {{after}}
  </h2>
</div>

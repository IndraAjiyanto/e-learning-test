<h2 class="text-4xl md:text-5xl font-bold leading-tight font-montserrat">

  {{!-- Teks sebelum highlight --}}
  {{before}}

  {{!-- Highlight dinamis --}}
  <span 
    style="background-color: {{bg}}; color: {{color}};"
  >
    {{highlight}}
  </span>

  {{!-- Teks setelah highlight --}}
  {{after}}

</h2>

{{!--
  Wrapper: Logbook User Detail Modal in Session Detail (Admin)
  Path: sub_partials/session/logbook_tab/modal_information.hbs
  Route: GET /session/:sessionId -> admin/session/detail.hbs (tab Logbook User)
  Implementation: organism components/ui/admin/logbook/logbook-detail-modal.hbs
  (composes logbook-detail-header + logbook-status-badge[existing] +
  logbook-detail-card + logbook-detail-field + logbook-detail-photo).
  Alpine scope (logbookData()): showModal, selectedLogbook, closeModal().
  BE: GET /session/logbooks/:sessionId → selectedLogbook { activity, activityDetails,
  obstacles, documentation, otherDocumentation, process, createdAt,
  user { username, email, linkedin?(future) / biodata?.linkedin?(future) } }.
  Kolom LinkedIn: User Information + tabel logbook-table (future-proof "-", entity
  User & Biodata belum punya kolom linkedin). Kolom Action: di logbook-table
  (View→openModal, Edit, Delete + Confirm Approve/Reject).
--}}
{{> components/ui/admin/logbook/logbook-detail-modal}}

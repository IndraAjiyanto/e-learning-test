          <!-- Info -->
          <div class='space-y-4'>
            <label
              class='block text-sm font-semibold text-gray-700 mb-1'
            >Info</label>
            <div class='space-y-6'>
              <!-- ID -->
              <div>
                <label
                  class='block text-xs font-medium text-gray-600 mb-2'
                >ID</label>
                <template
                  x-for='(info, index) in formData.infoId'
                  :key='index'
                >
                  <div class='flex items-center gap-3 mb-3'>
                    <input
                      type='text'
                      x-model='formData.infoId[index]'
                      :name="'infoId[]'"
                      placeholder='Enter info item (ID)'
                      class='flex-1 border border-gray-300 bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
                      @input='validateInfo("id", formData.infoId)'
                    />
                    <button
                      type='button'
                      @click='formData.infoId.splice(index,1); validateInfo("id", formData.infoId)'
                      x-show='formData.infoId.length > 1'
                      class='text-red-500 hover:text-red-700 font-bold text-lg w-8 h-8 flex items-center justify-center'
                    >
                      ✕
                    </button>
                  </div>
                </template>
                <button
                  type='button'
                  @click="formData.infoId.push(''); validateInfo('id', formData.infoId)"
                  class='w-full border-2 border-dashed border-gray-300 rounded-lg px-4 py-2 text-gray-500 hover:border-gray-500 hover:text-gray-700 flex items-center justify-center transition'
                >
                  + Add Info (ID)
                </button>

                <!-- Error Message -->
                <template x-if='formErrors.infoId'>
                  <div
                    class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg mt-2'
                  >
                    <i
                      class='fas fa-exclamation-circle text-red-500 mt-0.5'
                    ></i>
                    <p
                      class='text-xs text-red-700'
                      x-text='formErrors.infoId'
                    ></p>
                  </div>
                </template>
              </div>

              <!-- EN -->
              <div>
                <label
                  class='block text-xs font-medium text-gray-600 mb-2'
                >EN</label>
                <template
                  x-for='(info, index) in formData.infoEn'
                  :key='index'
                >
                  <div class='flex items-center gap-3 mb-3'>
                    <input
                      type='text'
                      x-model='formData.infoEn[index]'
                      :name="'infoEn[]'"
                      placeholder='Enter info item (EN)'
                      class='flex-1 border border-gray-300 bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
                      @input='validateInfo("en", formData.infoEn)'
                    />
                    <button
                      type='button'
                      @click='formData.infoEn.splice(index,1); validateInfo("en", formData.infoEn)'
                      x-show='formData.infoEn.length > 1'
                      class='text-red-500 hover:text-red-700 font-bold text-lg w-8 h-8 flex items-center justify-center'
                    >
                      ✕
                    </button>
                  </div>
                </template>
                <button
                  type='button'
                  @click="formData.infoEn.push(''); validateInfo('en', formData.infoEn)"
                  class='w-full border-2 border-dashed border-gray-300 rounded-lg px-4 py-2 text-gray-500 hover:border-gray-500 hover:text-gray-700 flex items-center justify-center transition'
                >
                  + Add Info (EN)
                </button>

                <!-- Error Message -->
                <template x-if='formErrors.infoEn'>
                  <div
                    class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg mt-2'
                  >
                    <i
                      class='fas fa-exclamation-circle text-red-500 mt-0.5'
                    ></i>
                    <p
                      class='text-xs text-red-700'
                      x-text='formErrors.infoEn'
                    ></p>
                  </div>
                </template>
              </div>

              <!-- JP -->
              <div>
                <label
                  class='block text-xs font-medium text-gray-600 mb-2'
                >JP</label>
                <template
                  x-for='(info, index) in formData.infoJa'
                  :key='index'
                >
                  <div class='flex items-center gap-3 mb-3'>
                    <input
                      type='text'
                      x-model='formData.infoJa[index]'
                      :name="'infoJa[]'"
                      placeholder='Enter info item (JP)'
                      class='flex-1 border border-gray-300 bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-gray-900 text-gray-700'
                      @input='validateInfo("ja", formData.infoJa)'
                    />
                    <button
                      type='button'
                      @click='formData.infoJa.splice(index,1); validateInfo("ja", formData.infoJa)'
                      x-show='formData.infoJa.length > 1'
                      class='text-red-500 hover:text-red-700 font-bold text-lg w-8 h-8 flex items-center justify-center'
                    >
                      ✕
                    </button>
                  </div>
                </template>
                <button
                  type='button'
                  @click="formData.infoJa.push(''); validateInfo('ja', formData.infoJa)"
                  class='w-full border-2 border-dashed border-gray-300 rounded-lg px-4 py-2 text-gray-500 hover:border-gray-500 hover:text-gray-700 flex items-center justify-center transition'
                >
                  + Add Info (JP)
                </button>

                <!-- Error Message -->
                <template x-if='formErrors.infoJa'>
                  <div
                    class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg mt-2'
                  >
                    <i
                      class='fas fa-exclamation-circle text-red-500 mt-0.5'
                    ></i>
                    <p
                      class='text-xs text-red-700'
                      x-text='formErrors.infoJa'
                    ></p>
                  </div>
                </template>
              </div>
            </div>
          </div>
        <!-- Isi per Bahasa -->
        <div class='space-y-4'>
          <label
            class='block text-sm font-semibold text-gray-700 mb-1'
          >Description</label>
          <div class='grid grid-cols-1 md:grid-cols-3 gap-4'>
            <!-- ID -->
            <div class='space-y-2'>
              <label class='block text-xs font-medium text-gray-600'>ID</label>
              <textarea
                name='content[id]'
                rows='4'
                required
                placeholder='Deskripsi (ID)...'
                :class="formErrors.content.id ? 'border-red-500 focus:ring-red-500' : formValid.content.id ? 'border-green-500 focus:ring-green-500' : 'border-gray-300 focus:ring-gray-900'"
                class='w-full border bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 text-gray-700 transition-colors'
                x-model='formData.content.id'
                @input='validateField("content", "id", $event.target.value)'
              ></textarea>

              <!-- Error Message -->
              <template x-if='formErrors.content.id'>
                <div
                  class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg'
                >
                  <i class='fas fa-exclamation-circle text-red-500 mt-0.5'></i>
                  <p
                    class='text-xs text-red-700'
                    x-text='formErrors.content.id'
                  ></p>
                </div>
              </template>

              <!-- Success Message -->
              <template x-if='formValid.content.id && !formErrors.content.id'>
                <div
                  class='flex items-center gap-2 p-2 bg-green-50 border border-green-300 rounded-lg'
                >
                  <i class='fas fa-check-circle text-green-500'></i>
                  <p class='text-xs text-green-700'>Valid description</p>
                </div>
              </template>
            </div>

            <!-- EN -->
            <div class='space-y-2'>
              <label class='block text-xs font-medium text-gray-600'>EN</label>
              <textarea
                name='content[en]'
                rows='4'
                required
                placeholder='Description (EN)...'
                :class="formErrors.content.en ? 'border-red-500 focus:ring-red-500' : formValid.content.en ? 'border-green-500 focus:ring-green-500' : 'border-gray-300 focus:ring-gray-900'"
                class='w-full border bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 text-gray-700 transition-colors'
                x-model='formData.content.en'
                @input='validateField("content", "en", $event.target.value)'
              ></textarea>

              <!-- Error Message -->
              <template x-if='formErrors.content.en'>
                <div
                  class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg'
                >
                  <i class='fas fa-exclamation-circle text-red-500 mt-0.5'></i>
                  <p
                    class='text-xs text-red-700'
                    x-text='formErrors.content.en'
                  ></p>
                </div>
              </template>

              <!-- Success Message -->
              <template x-if='formValid.content.en && !formErrors.content.en'>
                <div
                  class='flex items-center gap-2 p-2 bg-green-50 border border-green-300 rounded-lg'
                >
                  <i class='fas fa-check-circle text-green-500'></i>
                  <p class='text-xs text-green-700'>Valid description</p>
                </div>
              </template>
            </div>

            <!-- JA -->
            <div class='space-y-2'>
              <label class='block text-xs font-medium text-gray-600'>JA</label>
              <textarea
                name='content[ja]'
                rows='4'
                required
                placeholder='説明 (JA)...'
                :class="formErrors.content.ja ? 'border-red-500 focus:ring-red-500' : formValid.content.ja ? 'border-green-500 focus:ring-green-500' : 'border-gray-300 focus:ring-gray-900'"
                class='w-full border bg-white rounded-lg px-4 py-2 focus:outline-none focus:ring-2 text-gray-700 transition-colors'
                x-model='formData.content.ja'
                @input='validateField("content", "ja", $event.target.value)'
              ></textarea>

              <!-- Error Message -->
              <template x-if='formErrors.content.ja'>
                <div
                  class='flex items-start gap-2 p-2 bg-red-50 border border-red-300 rounded-lg'
                >
                  <i class='fas fa-exclamation-circle text-red-500 mt-0.5'></i>
                  <p
                    class='text-xs text-red-700'
                    x-text='formErrors.content.ja'
                  ></p>
                </div>
              </template>

              <!-- Success Message -->
              <template x-if='formValid.content.ja && !formErrors.content.ja'>
                <div
                  class='flex items-center gap-2 p-2 bg-green-50 border border-green-300 rounded-lg'
                >
                  <i class='fas fa-check-circle text-green-500'></i>
                  <p class='text-xs text-green-700'>Valid description</p>
                </div>
              </template>
            </div>
          </div>
        </div>

      <!-- Tabs -->
      <div class='border-b border-t border-slate-300 flex justify-center items-center gap-20 text-base font-medium overflow-x-auto'>
        <button
          @click="switchTab('participants')"
          :class="activeTab === 'participants' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class='transition-colors py-2 whitespace-nowrap'
        >
          {{t "test.bdetail.tab.participants"}}
        </button>
        <button
          @click="switchTab('what_you_learn')"
          :class="activeTab === 'what_you_learn' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.whatYouLearn"}}
        </button>
        <button
          @click="switchTab('benefits')"
          :class="activeTab === 'benefits' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.benefits"}}
        </button>
        <button
          @click="switchTab('learning_flow')"
          :class="activeTab === 'learning_flow' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.learningFlow"}}
        </button>
        <button
          @click="switchTab('schedule_fees')"
          :class="activeTab === 'schedule_fees' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.scheduleFees"}}
        </button>
        <button
          @click="switchTab('gallery')"
          :class="activeTab === 'gallery' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.gallery"}}
        </button>
        <button
          @click="switchTab('faq')"
          :class="activeTab === 'faq' ? 'text-secondary font-semibold' : 'text-slate-500 hover:text-secondary'"
          class=' transition-colors whitespace-nowrap'
        >
          {{t "test.bdetail.tab.faq"}}
        </button>
      </div>
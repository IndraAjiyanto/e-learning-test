<!-- CTA SECTION -->
<section id="explore-program" class="w-full bg-[#FAFAFA] font-montserrat relative">

  <!-- 1. KONTEN KHUSUS MOBILE (< 640px) -->
  <div class="block sm:hidden w-full py-12 px-5 text-center flex flex-col items-center justify-center">
    
    <!-- Icon Mobile -->
    <div class="mb-6 text-[#003060] flex items-center justify-center text-[48px] leading-none">
      <i class="fa-solid fa-users"></i>
    </div>

    <!-- Main Headline (Montserrat 600, text-4xl / 36px, leading-none) -->
    <h2 class="font-montserrat font-semibold text-2xl sm:text-3xl lg:text-4xl leading-none text-black text-center mb-12">
      <span> 1 + </span> {{t 'test.categoryPaidProgram.cta.title'}}
    </h2>

    <!-- Subtitle (Open Sans 400, text-[25px], leading-none) -->
    <p class="font-sans font-normal text-lg sm:text-xl lg:text-[25px] leading-none text-black/80 text-center mb-12">
      {{t 'test.categoryPaidProgram.cta.description'}}
    </p>

    <!-- CTA Button Mobile -->
    <div class="w-full max-w-[340px]">
      <a
        href="#programs"
        class="inline-flex items-center justify-center w-full h-[60px] gap-2.5 bg-[#003060] active:bg-[#001f3f] text-white font-montserrat font-semibold text-base rounded-[12px] transition-all duration-300 shadow-md"
      >
        <span>{{t 'test.categoryPaidProgram.cta.button'}}</span>
        <i class="fa-solid fa-arrow-up text-[23px]"></i>
      </a>
    </div>

  </div>

  <!-- 2. KONTEN KHUSUS DESKTOP (>= 640px / Figma Baseline W1728) -->
  <div class="hidden sm:block py-20">
    <div class="container mx-auto px-6 max-w-6xl text-center flex flex-col items-center justify-center">
      
      <!-- Icon Desktop -->
      <div class="mb-8 text-[#003060] flex items-center justify-center text-[60px] leading-none">
        <i class="fa-solid fa-users"></i>
      </div>

      <!-- Main Headline Desktop -->
      <h2 class="font-montserrat font-semibold text-2xl sm:text-3xl lg:text-4xl leading-none text-black text-center mb-12">
        <span> 1 + </span> Graduates Successfully Working in Digital Technology
      </h2>

      <!-- Subtitle Desktop -->
      <p class="font-sans font-normal text-lg sm:text-xl lg:text-[25px] leading-none text-black/80 text-center mb-12">
        Now It's Your Turn
      </p>

      <!-- CTA Button Desktop -->
      <div>
        <a
          href="#programs"
          class="inline-flex items-center justify-center w-[370px] h-[85px] gap-3 bg-[#003060] hover:bg-[#001f3f] text-white font-montserrat font-semibold text-base sm:text-2xl px-4 py-4 rounded-[12px] transition-all duration-300 shadow-md hover:shadow-lg"
        >
          <span>Explore Learning Path</span>
          <i class="fa-solid fa-arrow-up text-[23px]"></i>
        </a>
      </div>

    </div>
  </div>

</section>

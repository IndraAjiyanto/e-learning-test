{{!--
  Component: Form Label (Admin) — atomic
  Path: components/ui/admin/form/form-label.hbs
  Props: {{text}}, {{for}} (optional)

--}}
<label {{#if for}}for='{{for}}'{{/if}} class='text-[14px]/[normal] text-[#404040] font-inter font-medium'>{{text}}</label>

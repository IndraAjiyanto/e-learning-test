<div class="px-6 py-10 w-full mx-auto">
<div class="mx-auto">

  <div class="mb-8 bg-white rounded-xl shadow-lg p-6">
    <div class="mb-4">
      <a href="/week/{{session.weeks.id}}" 
         class="inline-flex items-center text-gray-700 text-base font-semibold hover:underline">
        <svg xmlns="http://www.w3.org/2000/svg" 
             class="h-5 w-5 mr-1" fill="none" 
             viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" 
                stroke-linejoin="round" 
                stroke-width="2" 
                d="M15 19l-7-7 7-7" />
        </svg>
        Back
      </a>
    </div>  

    <div class="flex items-start justify-between">
      <div>
        <h1 class="text-3xl font-bold mb-2 text-gray-900">Session {{session.pertemuan_ke}}</h1>

        <p class="text-gray-600 mb-1">
          <span class="font-semibold">Program:</span> {{session.weeks.course.name}}
        </p>
        <p class="text-gray-600 mb-1">
          <span class="font-semibold">Location:</span> {{session.location}}
        </p class="text-gray-600">
        <p class="text-gray-600 mb-1">
          <span class="font-semibold">Topic:</span> {{session.topic}}
        </p>
        <p class="text-gray-600 mb-1">
          <span class="font-semibold">Date:</span> {{formatTanggal session.date}}
        </p>
        <p class="text-gray-600 mb-1">
          <span class="font-semibold">Time :</span> {{formatTime session.start_time}} - {{formatTime session.end_time}}
        </p>

      </div>

<div class="flex items-center gap-4">
  <!-- Tombol Edit -->
  <a href="/session/formEdit/{{session.id}}"
     class="p-3 rounded-xl bg-blue-50 text-blue-600 hover:bg-blue-100 hover:text-blue-800 transition shadow-sm"
     title="Edit Pertemuan">
    <!-- Icon Edit (Pencil) -->
    <svg xmlns="http://www.w3.org/2000/svg"
         fill="none" viewBox="0 0 24 24"
         stroke-width="2" stroke="currentColor"
         class="w-7 h-7">
      <path stroke-linecap="round" stroke-linejoin="round"
            d="M16.862 4.487l1.687-1.688a1.875 1.875 
               0 112.652 2.652L10.582 16.07a4.5 
               4.5 0 01-1.897 1.13l-2.685.805.805-2.685
               a4.5 4.5 0 011.13-1.897l10.927-10.936z" />
      <path stroke-linecap="round" stroke-linejoin="round"
            d="M19.5 7.125L16.875 4.5" />
    </svg>
  </a>

  <!-- Tombol Delete -->
  <form action="/session/{{session.id}}/{{session.weeks.id}}?_method=DELETE" method="post">
    <button 
      type="button"
      onclick="confirmDelete(this)"
      class="p-3 rounded-xl bg-red-50 text-red-600 hover:bg-red-100 hover:text-red-800 transition shadow-sm"
      title="Delete Pertemuan">
      <!-- Icon Delete (X / Trash) -->
      <svg xmlns="http://www.w3.org/2000/svg"
           fill="none" viewBox="0 0 24 24"
           stroke-width="2" stroke="currentColor"
           class="w-7 h-7">
        <path stroke-linecap="round" stroke-linejoin="round"
              d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </form>
</div>

    </div>
  </div>
</div>


<div class="w-full mx-auto" x-data="tabData()">

  <div class="flex space-x-4 mb-6">
    <button @click="toggleFetch('/session/attendance/{{session.id}}', 'attendance')" 
            :class="tab === 'attendance' ? 'bg-gray-900 text-white' : 'bg-gray-100 text-gray-700'" 
            class="px-4 py-2 rounded-lg shadow transition">
      Attendance
    </button>
    <button @click="toggleFetch('', 'materi')" 
            :class="tab === 'materi' ? 'bg-gray-900 text-white' : 'bg-gray-100 text-gray-700'" 
            class="px-4 py-2 rounded-lg shadow transition">
      Materi
    </button>
    <button @click="toggleFetch('/session/task/{{session.id}}', 'assignment')" 
            :class="tab === 'assignment' ? 'bg-gray-900 text-white' : 'bg-gray-100 text-gray-700'" 
            class="px-4 py-2 rounded-lg shadow transition">
      Assignment
    </button>
    <button @click="toggleFetch('/session/logbook/{{session.id}}', 'logbook')" 
            :class="tab === 'logbook' ? 'bg-gray-900 text-white' : 'bg-gray-100 text-gray-700'" 
            class="px-4 py-2 rounded-lg shadow transition">
      Log book user
    </button>
    <button @click="toggleFetch('/session/logbook-mentor/{{session.id}}', 'logbook_mentor')" 
            :class="tab === 'logbook_mentor' ? 'bg-gray-900 text-white' : 'bg-gray-100 text-gray-700'" 
            class="px-4 py-2 rounded-lg shadow transition">
      Log book mentor
    </button>
  </div>

  <div x-show="tab === 'logbook'" x-data="logbookData()" x-init="fetchLogBook()">
    {{> session/logbook_tab}}
  </div>

  <div x-show="tab === 'logbook_mentor'" x-data="logBookMentorData()" x-init="fetchLogBookMentor()">
    {{> session/logbook_mentor_tab}}
  </div>

  <div x-show="tab === 'attendance'" x-data="absenData()" x-init="fetchAbsens()">
    {{> session/absen_tab}}
  </div>  

  <div x-show="tab === 'materi'" x-data="materiData()" x-init="fetchMateris()">
    {{> session/materi_tab}}
  </div>

  <div x-show="tab === 'assignment'" x-data="tugasData()" x-init="fetchTugas()">
    {{> session/tugas_tab}}
  </div>

</div>
</div>

<script
  src='https://cdn.sheetjs.com/xlsx-0.20.1/package/dist/xlsx.full.min.js'
></script>

<script>
function tabData() {
  return {
    search: '',
    tab: localStorage.getItem('activeTabSession'),
    data: localStorage.getItem('activeData')
      ? JSON.parse(localStorage.getItem('activeData'))
      : [],


    async init() {
      if(!this.tab) {
          this.tab = 'attendance';
          localStorage.setItem('activeTabSession', 'attendance');
      }
    },
    
    async toggleFetch(url, tabName) {
      this.tab = tabName;
      localStorage.setItem('activeTabSession', tabName);
      
      try {
        if(url === '') {
          this.data = [];
          localStorage.removeItem('activeData');
          return;
        }else{
          const response = await fetch(url);
        if (!response.ok) throw new Error('Network error');
        
        this.data = await response.json();
        localStorage.setItem('activeData', JSON.stringify(this.data));
        }
      } catch (error) {
      }
    }
  }
}
</script>

<div
  data-animate="fade-up" data-delay="100"
  class='hero px-4 py-16 bg-gradient-to-br from-white via-slate-50 to-slate-100 min-h-screen relative overflow-hidden'
>

  <div
    class='hero-content flex-col lg:flex-row-reverse gap-12 lg:gap-20 xl:gap-24 max-w-7xl mx-auto relative z-10'
  >

    <div class='relative flex-1 py-8'>
      <!-- Animated Rings Background - Behind Everything -->
      <div
        class='absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] border-2 border-slate-300 rounded-full opacity-10 animate-ping-slow pointer-events-none'
      ></div>
      <div
        class='absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[400px] h-[400px] border-2 border-slate-400 rounded-full opacity-10 animate-ping-slow animation-delay-1000 pointer-events-none'
      ></div>

      <!-- Orbiting Icons - Positioned Outside Image -->
      <div
        class='absolute top-12 -right-4 w-12 h-12 bg-white rounded-full shadow-lg flex items-center justify-center animate-float animation-delay-1000 z-10'
      >
        <i class='fas fa-code text-slate-700 text-lg'></i>
      </div>
      <div
        class='absolute bottom-60 -left-4 w-12 h-12 bg-white rounded-full shadow-lg flex items-center justify-center animate-float animation-delay-2000 z-10'
      >
        <i class='fas fa-laptop-code text-slate-700 text-lg'></i>
      </div>
      <div
        class='absolute top-10 -left-6 w-10 h-10 bg-white rounded-full shadow-lg flex items-center justify-center animate-bounce-subtle animation-delay-3000 z-10'
      >
        <i class='fas fa-graduation-cap text-slate-700'></i>
      </div>
      <div
        class='absolute bottom-1/3 -right-6 w-10 h-10 bg-white rounded-full shadow-lg flex items-center justify-center animate-bounce-subtle z-10'
      >
        <i class='fas fa-rocket text-slate-700'></i>
      </div>



      <!-- Main Image Container -->
      <div class='relative group mx-auto max-w-lg'>
        <!-- Multi-layer Glow -->
        <div
          class='absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-72 h-72 sm:w-80 sm:h-80 md:w-96 md:h-96 bg-gradient-to-r from-slate-400 to-slate-600 rounded-2xl blur-2xl opacity-10 group-hover:opacity-20 transition-opacity duration-500 animate-pulse-slow pointer-events-none'
        ></div>

        <!-- Decorative Bottom Wave -->
        <div class='absolute bottom-0 left-0 right-0 h-24 overflow-hidden pointer-events-none z-20'>
          <!-- Wave SVG -->
          <svg class='absolute bottom-0 w-full h-full' xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1440 320' preserveAspectRatio='none'>
            <path fill='rgba(241, 245, 249, 0.9)' fill-opacity='1' d='M0,96L48,112C96,128,192,160,288,165.3C384,171,480,149,576,128C672,107,768,85,864,90.7C960,96,1056,128,1152,138.7C1248,149,1344,139,1392,133.3L1440,128L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z'></path>
          </svg>
          
          <!-- Gradient Overlay -->
          <div class='absolute bottom-0 left-0 right-0 h-16 bg-gradient-to-t from-slate-50 to-transparent'></div>
        </div>

        <!-- Decorative Shapes -->
        <div class='absolute -bottom-4 left-8 w-16 h-16 bg-slate-200/40 rounded-full blur-xl animate-pulse-slow animation-delay-1000 pointer-events-none z-10'></div>
        <div class='absolute -bottom-6 right-12 w-20 h-20 bg-slate-300/30 rounded-full blur-2xl animate-pulse-slow animation-delay-2000 pointer-events-none z-10'></div>
        
        <!-- Abstract Geometric Shapes -->
        <div class='absolute bottom-8 left-4 w-8 h-8 border-2 border-slate-300/50 rounded-lg rotate-45 animate-float animation-delay-1000 pointer-events-none z-10'></div>
        <div class='absolute bottom-12 right-8 w-6 h-6 bg-slate-400/20 rounded-full animate-bounce-subtle animation-delay-2000 pointer-events-none z-10'></div>

        <img
          src='{{#if tentang.[0].gambar}}{{tentang.[0].gambar}}{{else}}/public/image/orang.png{{/if}}'
          class='relative w-full transform transition-transform duration-300 z-10'
        />

      </div>

    </div>

    <div
      class='text-gray-900 text-center h-fit lg:text-left flex-1 max-w-xl mt-16 lg:mt-0 grid-rows-[auto_1fr_auto_auto] gap-4'
    >
      <!-- Badge Above Title -->
      <div
        class='inline-flex  items-center gap-2 bg-gradient-to-r from-slate-100 to-slate-200 text-slate-700 px-5 py-2 rounded-full mb-6 shadow-md'
      >
        <i class='fas fa-award text-slate-600'></i>
        <span class='font-bold text-sm'>{{t 'test.home.heroSection.platform'}}</span>
      </div>
      <!-- Badge Above Title -->

      <h1 class='text-[3.2rem] font-bold leading-tight font-montserrat mb-4'>
        <div 
          class='bg-gradient-to-r from-slate-700 via-slate-800 to-slate-900 bg-clip-text text-transparent'
          data-texttype
          data-texts='["{{getByLang tentang.[0].judul lang }}"]'
        >
          {{#if tentang.[0].judul}}
            {{getByLang tentang.[0].judul lang}}
          {{else}}
            Kesatria Academy Empowering Future Digital Tech Talent's
          {{/if}}
        </div>
      </h1>

      <p class='py-6 text-lg text-gray-700 leading-relaxed  mb-6'>
        {{#if tentang.[0].text}}
          {{{nl2br (getByLang tentang.[0].text lang)}}}
        {{else}}
        Be part of the next generation of digital talents. Kesatria Academy helps you learn technology in an interactive, relevant and engaging way. 
        {{/if}}
      </p>

      <div
        class='flex flex-col sm:flex-row gap-4 justify-center lg:justify-start'
      >
        <button
          onclick="document.getElementById('courses-section').scrollIntoView({ behavior: 'smooth', block: 'start' })"
          class=' btn text-xl bg-gradient-to-br from-primary via-secondary to-gray-900 text-white hover:from-primary hover:to-secondary px-8 sm:px-16 lg:px-20 border-0 shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 relative overflow-hidden cursor-pointer' 
        >
          <span class='relative z-10 flex items-center gap-2'>
            <i class='fas fa-rocket'></i>
            {{t 'test.home.heroSection.buttonStartLearning'}}
          </span>
        </button>
        <a href="/dashboard/about" @click="localStorage.setItem('activeSection', 'contact');"
          class=' btn text-xl bg-white hover:bg-slate-50 text-gray-800 hover:text-slate-900 px-8 sm:px-16 lg:px-20 border-2 border-secondary shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2'
        >
          <span class='flex items-center gap-2'>
            <i class='fas fa-comments'></i>
            {{t 'test.home.heroSection.buttonConsultation'}}
          </span>
        </a>
      </div>

      <!-- Trust Indicators -->
      <div
        class='flex items-center justify-center lg:justify-start gap-6 mt-8 flex-wrap'
      >
        <div class='flex items-center gap-2 text-slate-600'>
          <i class='fas fa-shield-alt text-slate-700'></i>
          <span class='text-sm font-semibold'>{{t 'test.home.heroSection.jobPlacment'}}</span>
        </div>
        <div class='flex items-center gap-2 text-slate-600'>
          <i class='fas fa-headset text-slate-700'></i>
          <span class='text-sm font-semibold'>{{t 'test.home.heroSection.support'}}</span>
        </div>
        <div class='flex items-center gap-2 text-slate-600'>
          <i class='fas fa-graduation-cap text-slate-700'></i>
          <span class='text-sm font-semibold'>{{t 'test.home.heroSection.capstoneProject'}}</span>
        </div>
      </div>
    </div>

  </div>
</div>

<style>
  @keyframes blob {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes bounce-subtle {
    0%,
    100% {
      transform: translateY(0px) rotate(12deg);
    }
    50% {
      transform: translateY(-10px) rotate(12deg);
    }
  }

  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.2;
    }
    50% {
      opacity: 0.3;
    }
  }

  @keyframes ping-slow {
    0% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.2;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.1);
      opacity: 0.1;
    }
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.2;
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-bounce-subtle {
    animation: bounce-subtle 2s ease-in-out infinite;
  }

  .animate-pulse-slow {
    animation: pulse-slow 3s ease-in-out infinite;
  }

  .animate-ping-slow {
    animation: ping-slow 4s ease-in-out infinite;
  }

  .animation-delay-1000 {
    animation-delay: 1s;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-3000 {
    animation-delay: 3s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }
</style>

<!-- Bootcamp Experience Section -->
<div
  data-animate="fade-up" data-delay="100"
  class='bg-gradient-to-br from-primary via-secondary to-gray-900 rounded-t-[8vw] py-20 px-6 lg:px-20'
>
  <!-- Title -->
  <div class='text-center max-w-5xl mx-auto mb-10'>


    <h2
      class='text-4xl md:text-5xl font-bold text-white leading-tight font-montserrat'
    >
  {{t 'test.home.experienceSection.tittle'}}
    </h2>

    <p
      class='py-6 text-lg text-white leading-relaxed max-w-2xl mx-auto '
    >
      {{t 'test.home.experienceSection.description'}}
    </p>
  </div>

  <!-- Grid Card -->
  <div
    data-stagger-container
    class='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 text-center max-w-6xl mx-auto'
  >

  {{#if our_experience.length}}
    {{#each our_experience}}
       <div
      data-stagger-item
      class='group p-6 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-xl hover:border-b-slate-800 border-b-8 hover:bg-white/10 transition-all duration-300 transform hover:-translate-y-2'
    >
      <div class='flex justify-center mb-6'>
        <div
          class='w-16 h-16 flex items-center justify-center rounded-full bg-white/5 backdrop-blur-md text-white text-2xl group-hover:from-slate-600 group-hover:to-slate-800 group-hover:text-white transition-all duration-300'
        >
          <i class='fas {{icon}}'></i>
        </div>
      </div>
      <h3 class='text-xl font-bold text-white mb-4'>
        {{getByLang title ../lang}}
      </h3>
      <p class='text-white text-lg leading-relaxed'>
{{getByLang description ../lang}}
      </p>
    </div>
    {{/each}}
{{else}}

    <!-- Card 1 -->
    <div
      data-stagger-item
      class='group p-6 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-xl hover:border-b-slate-800 border-b-8 hover:bg-white/10 transition-all duration-300 transform hover:-translate-y-2'
    >
      <div class='flex justify-center mb-6'>
        <div
          class='w-16 h-16 flex items-center justify-center rounded-full bg-white/5 backdrop-blur-md text-white text-2xl group-hover:from-slate-600 group-hover:to-slate-800 group-hover:text-white transition-all duration-300'
        >
          <i class='fas fa-book-open'></i>
        </div>
      </div>
      <h3 class='text-xl font-bold text-white mb-4'>
        Free Class to Discover Potential
      </h3>
      <p class='text-white text-lg leading-relaxed'>
Join our free class to explore your interests and discover your true potential in the IT field. Get hands-on experience with the basics of technology.
      </p>
    </div>

    <!-- Card 2 -->
    <div
      data-stagger-item
      class='group p-6 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-xl hover:border-b-slate-800 border-b-8 hover:bg-white/10 transition-all duration-300 transform hover:-translate-y-2'
    >
      <div class='flex justify-center mb-6'>
        <div
          class='w-16 h-16 flex items-center justify-center rounded-full bg-white/5 backdrop-blur-md text-white text-2xl group-hover:from-slate-600 group-hover:to-slate-800 group-hover:text-white transition-all duration-300'
        >
          <i class='fas fa-chalkboard-teacher'></i>
        </div>
      </div>
      <h3 class='text-xl font-bold text-white mb-4'>
        Expert Mentor Guidance
      </h3>
      <p class='text-white text-lg leading-relaxed'>
        Learn directly from experienced industry professionals who have worked in leading tech companies. Gain real-world insights, hands-on knowledge, and personalized.
      </p>
    </div>

    <!-- Card 3 -->
    <div
      data-stagger-item
      class='group p-6 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-xl hover:border-b-slate-800 border-b-8 hover:bg-white/10 transition-all duration-300 transform hover:-translate-y-2'
    >
      <div class='flex justify-center mb-6'>
        <div
          class='w-16 h-16 flex items-center justify-center rounded-full bg-white/5 backdrop-blur-md text-white text-2xl group-hover:from-slate-600 group-hover:to-slate-800 group-hover:text-white transition-all duration-300'
        >
          <i class='fas fa-briefcase'></i>
        </div>
      </div>
      <h3 class='text-xl font-bold text-white mb-4'>
        Career Preparation Support
      </h3>
      <p class='text-white text-lg leading-relaxed'>
Get full career support — from crafting your CV and portfolio to preparing for interviews — with guidance from our dedicated talent support team.
      </p>
    </div>
    {{/if}}
  </div>
</div>


<section
  class='bg-gradient-to-br from-slate-50 via-white to-slate-100 py-20 px-6 lg:px-20 relative'
>
  <div class='text-center mb-16'>
    <h2 class='text-4xl md:text-5xl font-bold text-gray-900 mb-4'>
      {{t 'test.home.imagine.tittle'}}
    </h2>
    <p class='text-lg text-gray-600 max-w-2xl mx-auto'>
{{t 'test.home.imagine.description'}}
    </p>
  </div>

  <div class='grid lg:grid-cols-2 gap-16 items-center max-w-6xl mx-auto'>
    <div class='space-y-6'>

      {{! Fallback (static) when no data is passed) }}
      <div
        data-animate="slide-right"
        data-delay="100"
        class='relative group rounded-2xl p-6 bg-white shadow-xl ring-2 ring-emerald-400/60 hover:ring-emerald-500 transition-all duration-300'
      >
        <div
          aria-hidden='true'
          class='pointer-events-none absolute inset-0 rounded-2xl bg-emerald-50/40 group-hover:bg-emerald-50/70 transition-colors duration-300'
        ></div>
        <div class='absolute -top-3 left-6'>
          <span
            class='inline-flex items-center gap-1 rounded-full bg-emerald-600 px-3 py-1 text-xs font-semibold text-white shadow-sm'
          >
            <i class='fa-solid fa-fire text-[10px]'></i>
            {{t 'test.home.imagine.mostWanted'}}
          </span>
        </div>
        <div class='relative flex items-start gap-5'>
          <div
            class='text-white text-2xl bg-emerald-600 p-4 rounded-xl shadow-md'
          >
            <i class='{{#if benefit_1.icon}}{{computeIcon benefit_1.icon}}{{else}}fas fa-briefcase{{/if}}'></i>
          </div>
          <div>
            <h3 class='text-2xl font-extrabold text-gray-900 mb-2'>
              {{#if benefit_1.judul}}{{getByLang benefit_1.judul lang}}{{else}}Job/Internship Placement{{/if}}
            </h3>
            <p class='text-gray-700 text-lg leading-relaxed'>
              {{#if benefit_1.text}}{{getByLang benefit_1.text lang}}{{else}}Get the opportunity for job or internship placement through our hiring partner network after completing the bootcamp. We’ll help you start your career with strong industry connections.{{/if}}
            </p>
            <span class='sr-only'>{{t 'test.home.imagine.spanMostWanted'}}</span>
          </div>
        </div>
      </div>

      <div
        data-animate="slide-right"
        data-delay="100"
        class='flex items-start gap-5 group rounded-2xl p-4 bg-white/70 hover:bg-white transition-colors duration-300 ring-1 ring-slate-200 hover:ring-slate-300 shadow-lg'
      >
        <div
          class='text-white text-2xl bg-gradient-to-br from-primary via-secondary to-gray-900 p-4 rounded-xl group-hover:bg-slate-700 group-hover:text-white transition-colors duration-300'
        >
          <i class='{{#if benefit_2.icon}}{{computeIcon benefit_2.icon}}{{else}}fas fa-users{{/if}}'></i>
        </div>
        <div>
          <h3 class='text-xl font-bold text-gray-900 mb-2'>
            {{#if benefit_2.judul}}{{getByLang benefit_2.judul lang}}{{else}}Capstone Project{{/if}}
          </h3>
          <p class='text-gray-600 leading-relaxed'>
            {{#if benefit_2.text}}{{getByLang benefit_2.text lang}}{{else}}Apply everything you’ve learned through a real-world capstone project that reflects real industry challenges — a strong proof of your technical and problem-solving skills.{{/if}}
          </p>
        </div>
      </div>

      <div
        data-animate="slide-right"
        data-delay="100"
        class='flex items-start gap-5 group rounded-2xl p-4 bg-white/70 hover:bg-white transition-colors duration-300 ring-1 ring-slate-200 hover:ring-slate-300 shadow-lg'
      >
        <div
          class='text-white text-2xl bg-gradient-to-br from-primary via-secondary to-gray-900 p-4 rounded-xl group-hover:bg-primary group-hover:text-white transition-colors duration-300'
        >
          <i class='{{#if benefit_3.icon}}{{computeIcon benefit_3.icon}}{{else}}fas fa-certificate{{/if}}'></i>
        </div>
        <div>
          <h3 class='text-xl font-bold text-gray-900 mb-2'>
            {{#if benefit_3.judul}}{{getByLang benefit_3.judul lang}}{{else}}Portfolio Building{{/if}}
          </h3>
          <p class='text-gray-600 leading-relaxed'>
            {{#if benefit_3.text}}{{getByLang benefit_3.text lang}}{{else}}Build a powerful portfolio showcasing your best work and projects throughout the bootcamp — your professional identity and key to standing out in the job market.{{/if}}
          </p>
        </div>
      </div>

    </div>

    <div class='grid grid-cols-2 gap-6'>
      <img
        data-animate="scale-up"
        data-delay="100"
        src='{{#if gambar_benefit_1.gambar}}{{gambar_benefit_1.gambar}}{{else}}https://images.unsplash.com/photo-1522071820081-009f0129c71c?w=800{{/if}}'
        class='rounded-2xl shadow-2xl col-span-2 h-64 object-cover border-4 border-white hover:scale-105 transition-transform duration-300'
        alt='Teamwork'
      />
      <img
        data-animate="scale-up"
        data-delay="100"
        src='{{#if gambar_benefit_2.gambar}}{{gambar_benefit_2.gambar}}{{else}}https://images.unsplash.com/photo-1498050108023-c5249f4df085?w=800{{/if}}'
        class='rounded-2xl shadow-2xl h-48 object-cover border-4 border-white hover:scale-105 transition-transform duration-300'
        alt='Laptop Work'
      />
      <img
        data-animate="scale-up"
        data-delay="100"
        src='{{#if gambar_benefit_3.gambar}}{{gambar_benefit_3.gambar}}{{else}}https://images.unsplash.com/photo-1531482615713-2afd69097998?w=800{{/if}}'
        class='rounded-2xl shadow-2xl h-48 object-cover border-4 border-white hover:scale-105 transition-transform duration-300'
        alt='Meeting'
      />
      <img
        data-animate="scale-up"
        data-delay="100"
        src='{{#if gambar_benefit_4.gambar}}{{gambar_benefit_4.gambar}}{{else}}https://images.unsplash.com/photo-1517694712202-14dd9538aa97?w=800{{/if}}'
        class='rounded-2xl shadow-2xl col-span-2 h-60 object-cover border-4 border-white hover:scale-105 transition-transform duration-300'
        alt='Coding'
      />
    </div>
  </div>
</section>

<div
  x-data="{
      tab: 'all',
      categories: ['all', {{#each kategori}}'{{this.nama_kategori}}',{{/each}}],
      selectedCategory: 'all',
      selectedMethod: 'all',
      searchQuery: '',
      courseCategories: ['all', {{#each jenis_kelas}}`{{this.nama_jenis_kelas}}`,{{/each}}],
      currentPage: 1,
      itemsPerPage: 6,
      categoryDropdownOpen: false,
      typeDropdownOpen: false,
      methodDropdownOpen: false,
      
      categoriesData: [
        { value: 'all', icon: 'fa-th', title: '{{t "test.home.programSection.allCategory"}}', desc: '{{t "test.home.programSection.deskripsiAllCategory"}}' },
        {{#each kategori}}
        { value: '{{this.nama_kategori}}', icon: `{{this.icon}}`, title: '{{this.nama_kategori}}', desc: `{{truncate (getByLang this.deskripsi ../lang) 10}}` },
        {{/each}}
      ],
      
      courseCategoriesData: [
        { value: 'all', icon: 'fa-layer-group', title: '{{t "test.home.programSection.allTypes"}}', desc: '{{t "test.home.programSection.deskripsiAllTypes"}}' },
        {{#each jenis_kelas}}
        { value: `{{this.nama_jenis_kelas }}`, icon: '{{computeIcon this.icon}}', title: `{{this.nama_jenis_kelas}}`, desc: `{{truncate (getByLang this.deskripsi ../lang) 10}}` },
        {{/each}}
      ],
      
      methodOptions: [
        { value: 'all', icon: 'fa-th', title: '{{t "test.home.programSection.allMethod"}}', desc: '{{t "test.home.programSection.deskripsiAllMethod"}}' },
        { value: 'online', icon: 'fa-laptop', title: '{{t "test.home.programSection.online"}}', desc: '{{t "test.home.programSection.deskripsiOnline"}}' },
        { value: 'offline', icon: 'fa-map-marker-alt', title: '{{t "test.home.programSection.offline"}}', desc: '{{t "test.home.programSection.deskripsiOffline"}}' }
      ],

      get selectedCategoryItem() {
        return this.categoriesData.find(c => c.value === this.tab) || this.categoriesData[0];
      },
      
      get selectedTypeItem() {
        return this.courseCategoriesData.find(c => c.value === this.selectedCategory) || this.courseCategoriesData[0];
      },

      get selectedMethodItem() {
        return this.methodOptions.find(m => m.value === this.selectedMethod) || this.methodOptions[0];
      },

      get filteredKelas() {
          return [...$refs.kelasList.children].filter(el => {
              const matchesTab = this.tab === 'all' || el.dataset.kategori === this.tab;
              const matchesCategory = this.selectedCategory === 'all' || 
                                    el.dataset.category?.toLowerCase() === this.selectedCategory.toLowerCase();
              const matchesMethod = this.selectedMethod === 'all' || 
                                   el.dataset.location?.toLowerCase() === this.selectedMethod.toLowerCase();
              const matchesSearch = this.searchQuery === '' || 
                                  el.dataset.nama?.toLowerCase().includes(this.searchQuery.toLowerCase()) ||
                                  el.dataset.deskripsi?.toLowerCase().includes(this.searchQuery.toLowerCase());
              
              return matchesTab && matchesCategory && matchesMethod && matchesSearch;
          })
      },

      get totalPages() {
          return Math.ceil(this.filteredKelas.length / this.itemsPerPage)
      },

      paginatedKelas(el) {
          const idx = this.filteredKelas.indexOf(el)
          return idx >= (this.currentPage - 1) * this.itemsPerPage &&
                 idx < this.currentPage * this.itemsPerPage
      },

      resetFilters() {
          this.selectedCategory = 'all';
          this.selectedMethod = 'all';
          this.tab = 'all';
          this.searchQuery = '';
          this.currentPage = 1;
      }
  }"
  class='bg-gradient-to-br from-primary via-secondary to-gray-900 py-20 px-6 lg:px-20 relative overflow-visible'
  x-init="$watch('tab', () => currentPage = 1); $watch('selectedMethod', () => currentPage = 1); $watch('selectedCategory', () => currentPage = 1); $watch('searchQuery', () => currentPage = 1)"
  id='courses-section'
>

  <!-- Header Section -->
  <div class='text-center mb-16 relative'>
       <h2 class="text-4xl md:text-5xl font-bold leading-tight font-montserrat text-white">{{t "test.home.programSection.tittle"}}</h2>

    <p class='text-lg mt-4 text-slate-300 max-w-2xl mx-auto relative z-10'>
      {{t "test.home.programSection.description"}}
    </p>
  </div>

  <!-- Filter Section -->
  <div class='max-w-6xl mx-auto mb-16 relative z-10 overflow-visible'>
    <div
      class='bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-8 shadow-2xl'
    >
      <!-- Filter Title -->
      <div class='flex items-center gap-3 mb-6'>
        <div
          class='w-1 h-8 bg-gradient-to-b from-slate-400 to-slate-600 rounded-full'
        ></div>
        <h3 class='text-2xl font-bold text-white'>{{t "test.home.programSection.find"}}</h3>
      </div>

      <!-- Filter Controls -->
      <div
        class='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6 relative z-50'
      >

        <!-- Category Filter -->
        <div @click.away='categoryDropdownOpen = false' class='group relative'>
          <label
            class='flex items-center gap-2 text-slate-300 font-semibold mb-3 text-sm  tracking-wide'
          >
            <i class='fas fa-filter text-slate-400'></i>
            {{t 'test.home.programSection.category'}}
          </label>

          <button
            @click='categoryDropdownOpen = !categoryDropdownOpen'
            type='button'
            class='w-full px-5 py-4 pr-12 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl text-white font-medium focus:outline-none focus:ring-2 focus:ring-slate-500 transition-all duration-300 hover:bg-white/10 text-left relative'
          >
            <div class='flex items-center gap-3'>
              <template x-if="selectedCategoryItem.value !== 'all'">
              <img  :src="selectedCategoryItem.icon" class="w-5 h-5 object-cover rounded" alt="icon">
              </template>
              <template x-if="selectedCategoryItem.value === 'all'">
              <i :class='selectedCategoryItem.icon' class='fas text-slate-400'></i>
              </template>
              <span x-text='selectedCategoryItem.title'></span>
            </div>
            <div
              class='absolute right-4 top-1/2 -translate-y-1/2 transition-transform duration-300'
              :class="categoryDropdownOpen ? 'rotate-180' : ''"
            >
              <i class='fas fa-chevron-down text-white text-lg'></i>
            </div>
          </button>

          <!-- Dropdown Menu -->
          <div
            x-show='categoryDropdownOpen'
            x-transition
            style='z-index: 9999'
            class='absolute w-full mt-2 bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden'
          >
            <template x-for='category in categoriesData' :key='category.value'>
              <button
                type='button'
                @click='tab = category.value; categoryDropdownOpen = false'
                class='w-full px-5 py-4 text-left hover:bg-slate-50 transition-colors duration-200 flex items-start gap-4 group/item'
                :class="tab === category.value ? 'bg-slate-100' : ''"
              >
                <div
                  class='w-10 h-10 rounded-full bg-secondary flex items-center justify-center flex-shrink-0 group-hover/item:bg-slate-200 transition-colors text-white'
                  :class="tab === category.value ? 'bg-gradient-to-br from-primary via-secondary to-gray-900' : ''"
                >
                <template x-if="category.value !== 'all'">
                  <img  :src="category.icon" class="w-8 h-8 object-cover rounded-full" alt="icon">
</template>
<template x-if="category.value === 'all'">
                  <i x-show="category.value === 'all'" :class='category.icon' class='fas'></i>
                  </template>
                </div>
                <div class='flex-1'>
                  <h4
                    class='font-bold text-gray-900 mb-1'
                    x-text='category.title'
                  ></h4>
                  <p class='text-sm text-gray-600' x-text='category.desc'></p>
                </div>
                <div x-show='tab === category.value' class='flex-shrink-0'>
                  <i class='fas fa-check text-slate-600'></i>
                </div>
              </button>
            </template>
          </div>
        </div>

        <!-- Course Type Filter -->
        <div @click.away='typeDropdownOpen = false' class='group relative'>
          <label
            class='flex items-center gap-2 text-slate-300 font-semibold mb-3 text-sm  tracking-wide'
          >
            <i class='fas fa-layer-group text-slate-400'></i>
            {{t 'test.home.programSection.programType'}}
          </label>

          <button
            @click='typeDropdownOpen = !typeDropdownOpen'
            type='button'
            class='w-full px-5 py-4 pr-12 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl text-white font-medium focus:outline-none focus:ring-2 focus:ring-slate-500 transition-all duration-300 hover:bg-white/10 text-left relative'
          >
            <div class='flex items-center gap-3'>
              <i :class='selectedTypeItem.icon' class='fas text-slate-400'></i>
              <span x-text='selectedTypeItem.title'></span>
            </div>
            <div
              class='absolute right-4 top-1/2 -translate-y-1/2 transition-transform duration-300'
              :class="typeDropdownOpen ? 'rotate-180' : ''"
            >
              <i class='fas fa-chevron-down text-white text-lg'></i>
            </div>
          </button>

          <div
            x-show='typeDropdownOpen'
            x-transition
            style='z-index: 9999'
            class='absolute w-full mt-2 bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden max-h-96 overflow-y-auto'
          >
            <template x-for='type in courseCategoriesData' :key='type.value'>
              <button
                type='button'
                @click='selectedCategory = type.value; typeDropdownOpen = false'
                class='w-full px-5 py-4 text-left hover:bg-slate-50 transition-colors duration-200 flex items-start gap-4 group/item'
                :class="selectedCategory === type.value ? 'bg-slate-100' : ''"
              >
                <div
                  class='w-10 h-10 rounded-full bg-secondary flex items-center justify-center text-white flex-shrink-0 group-hover/item:bg-slate-200 transition-colors'
                  :class="selectedCategory === type.value ? 'bg-gradient-to-br from-primary via-secondary to-gray-900' : ''"
                >
                  <i :class='type.icon' class='fas'></i>
                </div>
                <div class='flex-1'>
                  <h4
                    class='font-bold text-gray-900 mb-1'
                    x-text='type.title'
                  ></h4>
                  <p class='text-sm text-gray-600' x-text='type.desc'></p>
                </div>
                <div
                  x-show='selectedCategory === type.value'
                  class='flex-shrink-0'
                >
                  <i class='fas fa-check text-slate-600'></i>
                </div>
              </button>
            </template>
          </div>
        </div>

        <!-- Method Filter (Online/Offline) -->
        <div @click.away='methodDropdownOpen = false' class='group relative'>
          <label
            class='flex items-center gap-2 text-slate-300 font-semibold mb-3 text-sm  tracking-wide'
          >
            <i class='fas fa-laptop text-slate-400'></i>
            {{t 'test.home.programSection.method'}}
          </label>

          <button
            @click='methodDropdownOpen = !methodDropdownOpen'
            type='button'
            class='w-full px-5 py-4 pr-12 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl text-white font-medium focus:outline-none focus:ring-2 focus:ring-slate-500 transition-all duration-300 hover:bg-white/10 text-left relative'
          >
            <div class='flex items-center gap-3'>
              <i
                :class='selectedMethodItem.icon'
                class='fas text-slate-400'
              ></i>
              <span x-text='selectedMethodItem.title'></span>
            </div>
            <div
              class='absolute right-4 top-1/2 -translate-y-1/2 transition-transform duration-300'
              :class="methodDropdownOpen ? 'rotate-180' : ''"
            >
              <i class='fas fa-chevron-down text-white text-lg'></i>
            </div>
          </button>

          <!-- Method Dropdown -->
          <div
            x-show='methodDropdownOpen'
            x-transition
            style='z-index: 9999'
            class='absolute w-full mt-2 bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden'
          >
            <template x-for='method in methodOptions' :key='method.value'>
              <button
                type='button'
                @click='selectedMethod = method.value; methodDropdownOpen = false'
                class='w-full px-5 py-4 text-left hover:bg-slate-50 transition-colors duration-200 flex items-start gap-4 group/item'
                :class="selectedMethod === method.value ? 'bg-slate-100' : ''"
              >
                <div
                  class='w-10 h-10 rounded-full bg-secondary flex items-center justify-center text-white flex-shrink-0 group-hover/item:bg-slate-200 transition-colors'
                  :class="selectedMethod === method.value ? 'bg-gradient-to-br from-primary via-secondary to-gray-900' : ''"
                >
                  <i :class='method.icon' class='fas'></i>
                </div>
                <div class='flex-1'>
                  <h4
                    class='font-bold text-gray-900 mb-1'
                    x-text='method.title'
                  ></h4>
                  <p class='text-sm text-gray-600' x-text='method.desc'></p>
                </div>
                <div
                  x-show='selectedMethod === method.value'
                  class='flex-shrink-0'
                >
                  <i class='fas fa-check text-slate-600'></i>
                </div>
              </button>
            </template>
          </div>
        </div>

        <!-- Search Input -->
        <div class='group'>
          <label
            class='flex items-center gap-2 text-slate-300 font-semibold mb-3 text-sm  tracking-wide'
          >
            <i class='fas fa-search text-slate-400'></i>
            {{t 'test.home.programSection.search'}}
          </label>
          <div class='relative'>
            <input
              type='text'
              x-model='searchQuery'
              placeholder='{{t "test.home.programSection.placeholderSearch"}}'
              class='w-full px-5 py-4 pr-12 bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl text-white placeholder-slate-400 font-medium focus:outline-none focus:ring-2 focus:ring-slate-500 transition-all duration-300 hover:bg-white/10'
            />
            <button
              x-show="searchQuery !== ''"
              @click="searchQuery = ''"
              class='absolute right-4 top-1/2 -translate-y-1/2 text-slate-400 hover:text-white transition-colors duration-200'
            >
              <i class='fas fa-times-circle text-xl'></i>
            </button>
          </div>
        </div>
      </div>

      <!-- Active Filters & Reset -->
      <div
        class='flex items-center justify-between flex-wrap gap-4'
        x-show="selectedCategory !== 'all' || tab !== 'all' || searchQuery !== '' || selectedMethod !== 'all'"
      >
        <div class='flex items-center gap-3 flex-wrap'>
          <span class='text-slate-400 text-sm font-medium'>{{t "test.home.programSection.ActiveFilters"}}</span>
          <span
            x-show="tab !== 'all'"
            class='px-4 py-2 bg-slate-700/50 text-slate-200 rounded-lg text-sm font-medium'
          >
            <i class='fas fa-tag mr-2'></i><span x-text='tab'></span>
          </span>
          <span
            x-show="selectedCategory !== 'all'"
            class='px-4 py-2 bg-slate-700/50 text-slate-200 rounded-lg text-sm font-medium'
          >
            <i class='fas fa-layer-group mr-2'></i><span
              x-text='selectedCategory'
            ></span>
          </span>
          <span
            x-show="selectedMethod !== 'all'"
            class='px-4 py-2 bg-slate-700/50 text-slate-200 rounded-lg text-sm font-medium'
          >
            <i :class="'fas ' + selectedMethodItem.icon + ' mr-2'"></i><span
              x-text='selectedMethodItem.title'
            ></span>
          </span>
          <span
            x-show="searchQuery !== ''"
            class='px-4 py-2 bg-slate-700/50 text-slate-200 rounded-lg text-sm font-medium'
          >
            <i class='fas fa-search mr-2'></i>"<span
              x-text='searchQuery'
            ></span>"
          </span>
        </div>
        <button
          @click='resetFilters()'
          class='px-6 py-2.5 bg-gradient-to-br from-primary via-secondary to-gray-900 hover:from-primary hover:to-secondary text-white rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg font-medium'
        >
          <i class='fas fa-redo mr-2'></i>{{t "test.home.programSection.resetAll"}}
        </button>
      </div>
    </div>

    <!-- Results Counter -->
    <div class='mt-6 text-center'>
      <p class='text-slate-400'>
        {{t "test.home.programSection.showing"}}
        <span
          class='text-white font-bold'
          x-text='filteredKelas.filter((el, idx) => paginatedKelas(el)).length'
        ></span>
        {{t "test.home.programSection.of"}}
        <span class='text-white font-bold' x-text='filteredKelas.length'></span>
        {{t "test.home.programSection.program"}}
      </p>
    </div>
  </div>

  <!-- Classes Grid -->
  <div
    class='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto'
    x-ref='kelasList'
  >
    {{#each kelas}}
      <div
        data-animate="scale-up"
        data-delay="100"
        class='group card bg-white shadow-2xl border-0 rounded-2xl overflow-hidden transition-all duration-300 transform hover:-translate-y-2 hover:shadow-3xl'
        data-kategori='{{kategori.nama_kategori}}'
        data-category='{{ jenis_kelas.nama_jenis_kelas }}'
        data-nama='{{nama_kelas}}'
        data-deskripsi='{{getByLang deskripsi ../lang}}'
        data-location='{{metode}}'
        x-show="(tab === 'all' || tab === '{{kategori.nama_kategori}}') && paginatedKelas($el)"
        x-transition
      >
        <!-- Gambar lebih bescar -->
        <img
          src='{{gambar}}'
          alt='{{nama_kelas}}'
          class='h-48 w-full object-cover
 group-hover:scale-105 transition-transform duration-300 drop-shadow-lg'
        />

        <div class='card-body p-6'>
          <div class='flex gap-2 mb-3'>
            <span
              class='badge flex justify-center h-fit items-center bg-slate-100 text-slate-700 px-3 py-1 text-xs font-semibold rounded-full'
            >
              {{kategori.nama_kategori}}
            </span>
            <span
              class='badge flex justify-center h-fit items-center bg-gradient-to-br from-primary via-secondary to-gray-900  text-white px-3 py-1 text-xs font-semibold rounded-full'
            >
              {{ jenis_kelas.nama_jenis_kelas }}
            </span>

          </div>

          <div class='flex flex-wrap gap-2 mb-4 text-sm text-gray-700'>
            <div
              class='flex items-center gap-2 bg-gray-100 px-3 py-1 rounded-full'
            >
            {{#if (eq metode "online")}}
              <i class='fas fa-laptop text-slate-500'></i>
              <span>{{t "test.home.programSection.online"}}</span>
              {{else if (eq metode "offline")}}
              <i class='fas fa-map-marker-alt text-slate-500'></i>
              <span>{{t "test.home.programSection.offline"}}</span>
              {{/if}}
            </div>
            <div
              class='flex items-center gap-2 bg-gray-100 px-3 py-1 rounded-full'
            >
              <i class='fas fa-user-tie text-slate-500'></i>
              <span>{{getByLang lokasi ../lang}}</span>
            </div>
          </div>

          <h2
            class='card-title text-gray-900 text-xl font-bold mb-3'
          >{{nama_kelas}}</h2>

          <p class='text-sm text-gray-600 line-clamp-1 mb-4 leading-relaxed'>
            {{getByLang deskripsi ../lang}}
          </p>

          <div
            class='bg-gradient-to-br from-slate-50 to-slate-100 rounded-lg p-4 mb-4 border border-slate-200'
          >
            <div class='flex items-center justify-between mb-3'>
              <div class='flex items-center gap-2'>
                <i class='fas fa-users text-slate-600 animate-pulse'></i>
                <span class='text-sm font-medium text-slate-600'>{{t "test.home.programSection.kuota"}}</span>
              </div>
              <span
                class='text-sm font-bold text-slate-800 bg-white px-3 py-1 rounded-full shadow-sm'
              >
                {{user_kelas.length}}
                /
                {{kuota}}
                {{t "test.home.programSection.student"}}
              </span>
            </div>

            <div
              class='w-full bg-slate-200 rounded-full h-2 overflow-hidden mb-2'
            >
              <div
                class='h-2 rounded-full bg-gradient-to-r from-green-400 to-green-600 transition-all duration-500 ease-out'
                :style='`width: ${({{user_kelas.length}} / {{kuota}}) * 100}%`'
              >
              </div>
            </div>

          </div>

        <div class='card-actions'>
          <a
            href='/kelass/{{id}}'
            class='btn bg-gradient-to-br from-primary via-secondary to-gray-900 hover:from-primary hover:to-secondary text-white text-base px-8 py-3 w-full border-0 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 flex justify-center items-center text-center'
          >
            {{t "test.home.programSection.buttonViewProgram"}}
          </a>
        </div>
        </div>
      </div>
    {{/each}}

  </div>

  <!-- No Results -->
  <div
    x-show='filteredKelas.length === 0'
    class='text-center py-20'
    x-transition
  >
    <div
      class='bg-white/5 backdrop-blur-md border border-white/10 w-32 h-32 mx-auto mb-6 rounded-full flex items-center justify-center'
    >
      <i class='fas fa-inbox text-5xl text-slate-400'></i>
    </div>
    <h3 class='text-3xl font-bold text-white mb-3'>{{t "test.home.programSection.noProgram"}}</h3>
    <p class='text-slate-300 text-lg mb-6'>{{t "test.home.programSection.spanNoProgram"}}</p>
    <button
      @click='resetFilters()'
      class='btn text-lg border-0 bg-gradient-to-br from-primary via-secondary to-gray-900 hover:from-primary hover:to-secondary text-white rounded-lg transition-all duration-300 transform font-semibold shadow-xl hover:-translate-y-2 relative overflow-hidden cursor-pointer'
    >
      <i class='fas fa-redo mr-2'></i>{{t "test.home.programSection.clearFilters"}}
    </button>
  </div>

  <!-- Pagination -->
  <div
    class='flex justify-center items-center gap-4 mt-12'
    x-show='totalPages > 1'
  >
    <button
      @click='currentPage = Math.max(1, currentPage - 1)'
      :disabled='currentPage === 1'
      class='px-5 py-3 bg-slate-700 hover:bg-slate-600 text-white rounded-xl disabled:opacity-40 disabled:cursor-not-allowed transition-all duration-300'
    >
      <i class='fas fa-chevron-left'></i>
    </button>

    <template x-for='page in totalPages' :key='page'>
      <button
        @click='currentPage = page'
        class='w-12 h-12 rounded-xl text-lg font-semibold transition-all duration-300'
        :class="currentPage === page ? 'bg-white text-slate-800 shadow-xl' : 'bg-slate-700 text-white hover:bg-slate-600'"
        x-text='page'
      ></button>
    </template>

    <button
      @click='currentPage = Math.min(totalPages, currentPage + 1)'
      :disabled='currentPage === totalPages'
      class='px-5 py-3 bg-slate-700 hover:bg-slate-600 text-white rounded-xl disabled:opacity-40 disabled:cursor-not-allowed transition-all duration-300'
    >
      <i class='fas fa-chevron-right'></i>
    </button>
  </div>
</div>

<!-- Special Programs Section (WIP & In-House Training) -->
<section class='bg-gradient-to-br from-white via-gray-50 to-white py-20 px-6 lg:px-20'>
  <div class='container mx-auto max-w-7xl'>
    <!-- Header -->
    <div class='text-center mb-16 min-h-42'>

      <h2 class='text-4xl md:text-5xl font-bold text-gray-900 mb-4 font-montserrat'>
        {{t "test.home.specialProgram.tittle"}}
      </h2>
      <p class='text-lg text-gray-600 max-w-2xl mx-auto  mt-3'>
        {{t "test.home.specialProgram.description"}}
      </p>
    </div>

    <!-- Two Cards Grid -->
    <div class='grid md:grid-cols-2 gap-8 max-w-6xl mx-auto'>
      
      <!-- WIP Card -->
{{#each special_program}}
<a href='kategoris/program/{{nama_kategori}}' class='group block h-full' data-animate="slide-right" data-delay="100">
  <div class='relative bg-white rounded-3xl p-8 border-2 border-gray-300 hover:border-secondary shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 overflow-hidden h-full flex flex-col'>
    <!-- Background Pattern -->
    <div class='absolute top-0 right-0 w-64 h-64 bg-gradient-to-br from-gray-100/50 to-gray-200/50 rounded-full blur-3xl -z-0'></div>
    
    <div class='relative z-10 flex flex-col h-full'>
      <!-- Icon - Fixed height -->
      <div class='mb-6 flex items-center justify-between flex-shrink-0'>
        <div class='w-20 h-20 bg-gradient-to-br from-primary via-secondary to-gray-900 rounded-2xl flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300'>
          <img src='{{icon}}' alt='{{nama_kategori}}' class='w-12 h-12'>
        </div>
        <span class='bg-gradient-to-br from-primary via-secondary to-gray-900 text-white px-4 py-2 rounded-full text-sm font-bold whitespace-nowrap'>
          {{getByLang for ../lang}}
        </span>
      </div>

      <!-- Title - Fixed height with line clamp -->
      <h3 class='text-3xl font-bold text-gray-900 mb-4 font-montserrat group-hover:text-gray-700 transition-colors min-h-[4rem] line-clamp-2'>
        {{nama_kategori}}
      </h3>
      
      <!-- Description - Fixed height with line clamp -->
      <p class='text-gray-600 text-lg leading-relaxed mb-6 min-h-[6rem] line-clamp-3'>
        {{getByLang deskripsi ../lang}}
      </p>

      <!-- Features List - Fixed height with scroll if needed -->
      <ul class='space-y-3 mb-8 min-h-[12rem] max-h-[12rem] overflow-y-auto'>
        {{#if (eq ../lang 'id')}}
          {{#each info_id}}
          <li class='flex items-center gap-3 text-gray-700'>
            <div class='w-6 h-6 bg-gray-800 rounded-full flex items-center justify-center flex-shrink-0'>
              <i class='fas fa-check text-white text-xs'></i>
            </div>
            <span class='font-medium line-clamp-1'>{{this}}</span>
          </li>
          {{/each}}
        {{else if (eq ../lang 'en')}}
          {{#each info_en}}
          <li class='flex items-center gap-3 text-gray-700'>
            <div class='w-6 h-6 bg-gray-800 rounded-full flex items-center justify-center flex-shrink-0'>
              <i class='fas fa-check text-white text-xs'></i>
            </div>
            <span class='font-medium line-clamp-1'>{{this}}</span>
          </li>
          {{/each}}
        {{else if (eq ../lang 'ja')}}
          {{#each info_ja}}
          <li class='flex items-center gap-3 text-gray-700'>
            <div class='w-6 h-6 bg-gray-800 rounded-full flex items-center justify-center flex-shrink-0'>
              <i class='fas fa-check text-white text-xs'></i>
            </div>
            <span class='font-medium line-clamp-1'>{{this}}</span>
          </li>
          {{/each}}
        {{/if}}
      </ul>

      <!-- CTA Button - Always at bottom -->
      <div class='flex items-center justify-between pt-6 border-t-2 border-gray-200 mt-auto flex-shrink-0'>
        <span class='text-gray-600 font-semibold whitespace-nowrap'>{{t "test.home.programSection.buttonViewProgram"}}</span>
        <div class='w-12 h-12 bg-gradient-to-br from-primary via-secondary to-gray-900 hover:from-primary hover:to-secondary rounded-full flex items-center justify-center transition shadow-lg group-hover:scale-110 flex-shrink-0'>
          <i class="fas fa-arrow-right text-white transition-transform group-hover:scale-125"></i>
        </div>
      </div>
    </div>
  </div>
</a>
{{/each}}

    </div>

  </div>
</section>

<!-- Alumni Stories Section -->
<section
  class='bg-gradient-to-br from-primary via-secondary to-gray-900 py-20 px-6 lg:px-20'
  x-data='{
        active: 0, 
        slides: [0,1,2],
        init() {
            // Auto slide setiap 5 detik
            setInterval(() => {
                this.active = (this.active + 1) % this.slides.length;
            }, 5000);
        }
    }'
>
  <!-- Title -->
  <div class='text-center mb-4'>
      {{!-- {{!-- {{> highlightText 
        before="Stories from"
        highlight="Alumni"
        after=""
        textColor="text-gray-900"
        bg="bg-primary"
        color="text-white"
      }}  --}}

       <h2 class="text-4xl md:text-5xl font-bold leading-tight font-montserrat text-white">{{t "test.home.alumniSection.tittle"}}</h2>

    <p class='text-lg text-white py-6 max-w-2xl mx-auto'>
      {{t "test.home.alumniSection.description"}}
    </p>
  </div>

  <!-- Carousel -->
  <div class='relative w-full max-w-5xl mx-auto overflow-hidden'>

    {{#if alumni}}
    <!-- Slide Wrapper -->
    <div
      class='flex transition-transform duration-500'
      :style='`transform: translateX(-${active * 100}%);`'
    >

      <!-- Slide (Dynamic from data) -->
      {{#each alumni}}
        <div class='w-full flex-shrink-0 flex justify-center px-4'>
          <div
            class='bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-2xl p-10 max-w-3xl'
          >
            <span class='text-6xl text-white/50 font-bold'>❝</span>
            <p class='text-white text-xl leading-relaxed mb-8 italic'>
              {{getByLang pesan ../lang}}
            </p>
            <div class='flex items-center gap-6'>
              <img
                src='{{profile}}'
                alt='Alumni'
                class='w-16 h-16 rounded-full border-4 border-white/20'
              />
              <div>
                <p class='font-bold text-white text-lg'>{{getByLang nama ../lang}}</p>
                <p class='text-slate-300'>{{getByLang posisi_sekarang ../lang}}</p>
                <p class='text-slate-400 text-sm'>{{getByLang kelas.nama_kelas ../lang}}</p>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>

    <!-- Navigation Buttons -->
    <button
      class='absolute left-4 top-1/2 -translate-y-1/2 w-12 h-12 rounded-full bg-white/10 backdrop-blur-md text-white border border-white/20 hover:bg-white/20 hover:text-white shadow-lg transition-all duration-300'
      @click='active = (active - 1 + slides.length) % slides.length'
    >
      <i class='fas fa-chevron-left'></i>
    </button>
    <button
      class='absolute right-4 top-1/2 -translate-y-1/2 w-12 h-12 rounded-full bg-white/10 backdrop-blur-md text-white border border-white/20 hover:bg-white/20 hover:text-white shadow-lg transition-all duration-300'
      @click='active = (active + 1) % slides.length'
    >
      <i class='fas fa-chevron-right'></i>
    </button>

    <!-- Dot Indicators -->
    <div class='flex justify-center mt-8 gap-3'>
      <template x-for='(s, i) in slides' :key='i'>
        <button
          class='w-4 h-4 rounded-full transition-all duration-300'
          :class="active === i ? 'bg-white scale-125' : 'bg-white/50 hover:bg-white/70'"
          @click='active = i'
        ></button>
      </template>
    </div>
    {{else}}
    <!-- Fallback - No Alumni Data -->
    <div class='text-center py-12'>
      <div
        class='bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl shadow-2xl p-10 max-w-3xl mx-auto'
      >
        <span class='text-6xl text-white/50 font-bold'>❝</span>
        <p class='text-white text-xl leading-relaxed mb-8 italic'>
          This bootcamp transformed my career completely! The mentors were amazing, and the curriculum was very comprehensive. Now I work at a leading tech company with a great salary.
        </p>
        <div class='flex items-center gap-6 justify-center'>
          <img
            src='https://ui-avatars.com/api/?name=John+Doe&background=64748b&color=fff&size=128'
            alt='Alumni'
            class='w-16 h-16 rounded-full border-4 border-white/20'
          />
          <div class='text-left'>
            <p class='font-bold text-white text-lg'>John Doe</p>
            <p class='text-slate-300'>Senior Software Engineer</p>
            <p class='text-slate-400 text-sm'>Full Stack Development Bootcamp</p>
          </div>
        </div>
      </div>
    </div>
    {{/if}}
  </div>
</section>

<!-- Hiring Partners Section -->
<section class='bg-white py-20 px-6 lg:px-20'>
  <div class='text-center mb-16'>
    <h2 class="text-4xl md:text-5xl font-bold leading-tight font-montserrat text-gray-900">{{t "test.home.partnerSection.tittle"}}</h2>
    <p class='text-lg text-gray-600 py-6 max-w-2xl mx-auto'>
      {{t "test.home.partnerSection.description"}}
    </p>
  </div>

  <div class='overflow-hidden whitespace-nowrap bg-white rounded-2xl shadow-lg py-8 relative'>
    {{#if kerja_sama}}
    <div class=' inline-flex animate-scroll-seamless'>
      {{#each kerja_sama}}
      <img
        src='{{gambar}}'
        alt='Partner Logo'
        class='h-24 w-auto inline-block mx-8 grayscale hover:grayscale-0 transition-all duration-300 flex-shrink-0'
      />
      {{/each}}
    </div>
    {{else}}
    <div class='flex items-center justify-center gap-8 px-8'>
      <div class='text-center text-slate-400 py-4'>
        <i class='fas fa-building text-4xl mb-2'></i>
        <p class='text-sm'>Partner Logo 1</p>
      </div>
      <div class='text-center text-slate-400 py-4'>
        <i class='fas fa-industry text-4xl mb-2'></i>
        <p class='text-sm'>Partner Logo 2</p>
      </div>
      <div class='text-center text-slate-400 py-4'>
        <i class='fas fa-store text-4xl mb-2'></i>
        <p class='text-sm'>Partner Logo 3</p>
      </div>
      <div class='text-center text-slate-400 py-4'>
        <i class='fas fa-landmark text-4xl mb-2'></i>
        <p class='text-sm'>Partner Logo 4</p>
      </div>
      <div class='text-center text-slate-400 py-4'>
        <i class='fas fa-university text-4xl mb-2'></i>
        <p class='text-sm'>Partner Logo 5</p>
      </div>
    </div>
    {{/if}}
  </div>
</section>

<style>
@keyframes scroll-seamless {
  0% {
    transform: translateX(100%);
  }
  100% {
    transform: translateX(-200%);
  }
}

.animate-scroll-seamless {
  animation: scroll-seamless 30s linear infinite;
  will-change: transform;
}
</style>

<!-- FAQ Section -->
<section class='py-20 bg-gradient-to-br from-slate-50 via-white to-slate-100'>
  <div class='container mx-auto px-6 max-w-6xl'>

    <div class='max-w-4xl mx-auto space-y-8'>
       <h2 class="text-4xl md:text-5xl font-bold leading-tight text-center font-montserrat text-gray-900">{{t "test.home.faq.tittle"}}</h2>

      <div
        x-data="{
          activeIndex: null,
          currentPage: 1,
          itemsPerPage: 5,
          faqs: [
            {{#if faq}}
              {{#each faq}}
                { question: `{{getByLang question ../lang}}` , answer: `{{getByLang answer ../lang}}` },
              {{/each}}
            {{else}}
              { question: 'How long is the bootcamp program?', answer: 'Our bootcamp program typically runs for 4 months with intensive weekend classes. You\'ll learn through hands-on projects and mentorship from industry experts.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'Do I need prior programming experience?', answer: 'No prior experience is required! We welcome beginners and will guide you from the basics to advanced concepts step by step.' },
              { question: 'What is the payment method?', answer: 'We offer flexible payment options including full payment, installments, and early bird discounts. Contact us for more details.' }
            {{/if}}
          ],
          get paginatedFaqs() {
            const start = (this.currentPage - 1) * this.itemsPerPage;
            const end = start + this.itemsPerPage;
            return this.faqs.slice(start, end);
          },
          get totalPages() {
            return Math.ceil(this.faqs.length / this.itemsPerPage);
          },
          toggle(index) {
            this.activeIndex = this.activeIndex === index ? null : index;
          }
        }"
      >
        <!-- FAQ Items -->
        <template x-for="(faq, index) in paginatedFaqs" :key="index">
          <div
            class='group bg-white rounded-2xl shadow-lg border border-slate-200 hover:shadow-2xl hover:border-slate-300 transition-all duration-300 overflow-hidden mb-5'
          >
            <button
              @click="toggle((currentPage - 1) * itemsPerPage + index)"
              class='w-full text-left p-8 focus:outline-none'
            >
              <div class='flex items-center justify-between'>
                <h3
                  class='text-xl font-bold text-gray-900 group-hover:text-slate-700 transition-colors duration-300'
                  x-text="faq.question"
                ></h3>
                <div
                  class='w-8 h-8 flex items-center justify-center rounded-full bg-slate-100 group-hover:bg-slate-200 transition-colors duration-300'
                >
                  <i
                    class='fas fa-chevron-down text-slate-600 transition-transform duration-300'
                    :class="activeIndex === (currentPage - 1) * itemsPerPage + index ? 'rotate-180' : ''"
                  ></i>
                </div>
              </div>
            </button>
            <div
              x-show="activeIndex === (currentPage - 1) * itemsPerPage + index"
              x-transition:enter="transition ease-out duration-300"
              x-transition:enter-start="opacity-0 max-h-0"
              x-transition:enter-end="opacity-100 max-h-screen"
              x-transition:leave="transition ease-in duration-200"
              x-transition:leave-start="opacity-100 max-h-screen"
              x-transition:leave-end="opacity-0 max-h-0"
              class='overflow-hidden'
            >
              <div class='px-8 pb-8'>
                <p class='text-gray-600 leading-relaxed text-lg' x-text="faq.answer"></p>
              </div>
            </div>
          </div>
        </template>

        <!-- Pagination -->

<div class="flex items-center justify-center gap-3 flex-wrap">
            <!-- First Page -->
            <button
              @click="currentPage = 1"
              :disabled="currentPage === 1"
              class="p-2.5 rounded-lg hover:text-slate-400 text-slate-700 disabled:opacity-20 disabled:cursor-not-allowed transition-all duration-300"
            >
              <i class="fas fa-angles-left"></i>
            </button>

      

            <!-- Current Page Display -->
            <div class="px-6 py-3 text-gray-900 font-bold">
              <span class="text-xl" x-text="currentPage"></span>
              <span class="text-slate-300 mx-2">/</span>
              <span class="text-lg text-slate-300" x-text="totalPages"></span>
            </div>


            <!-- Last Page -->
            <button
              @click="currentPage = totalPages"
              :disabled="currentPage === totalPages"
              class="p-2.5 hover:text-slate-400 text-slate-700 disabled:opacity-20 disabled:cursor-not-allowed transition-all duration-300"
            >
              <i class="fas fa-angles-right"></i>
            </button>
          </div>
      </div>
    </div>
  </div>
</section>

<div
  x-data='{ showTooltip: false }'
  class='fixed bottom-6 right-6 md:bottom-8 md:right-8 z-50 group'
  @mouseenter='showTooltip = true'
  @mouseleave='showTooltip = false'
>
  <!-- Tooltip -->
  <div
    x-show='showTooltip'
    x-transition:enter='transition ease-out duration-200'
    x-transition:enter-start='opacity-0 translate-x-2'
    x-transition:enter-end='opacity-100 translate-x-0'
    x-transition:leave='transition ease-in duration-150'
    x-transition:leave-start='opacity-100 translate-x-0'
    x-transition:leave-end='opacity-0 translate-x-2'
    class='hidden md:block absolute right-20 top-1/2 -translate-y-1/2 bg-gradient-to-r from-slate-700 to-slate-800 text-white px-5 py-3 rounded-xl shadow-2xl whitespace-nowrap font-semibold text-sm'
  >
    {{t "test.home.needHelp.tittle"}}
    <div
      class='absolute -right-2 top-1/2 -translate-y-1/2 w-0 h-0 border-t-8 border-b-8 border-l-8 border-transparent border-l-slate-800'
    ></div>
  </div>

  <!-- Button -->
  <a
    href='https://wa.me/62{{social.[0].nomor}}?text=Halo%20Kesatria%20Academy!%20Saya%20ingin%20bertanya'
    target='_blank'
    class='relative flex items-center justify-center w-14 h-14 md:w-16 md:h-16 bg-gradient-to-br from-green-500 to-green-600 rounded-full shadow-lg hover:shadow-2xl hover:scale-110 hover:-translate-y-2 hover:from-green-400 hover:to-green-500 transition-all duration-300 animate-pulse-slate group'
    aria-label='Chat WhatsApp'
  >
    <i class='fab fa-whatsapp text-white text-2xl md:text-3xl'></i>


  </a>
</div>

{{!-- Dibungkus chrome backoffice. Halaman ini ditautkan dari dalam area
     belajar tetapi punya rute sendiri; sebelumnya memakai navbar publik
     dan footer. Controller-nya merender dengan bareShell: true.
     Lihat docs/user-area-learning-consolidation-plan.md tahap 2. --}}
{{#> user/shell_frame/index user=user active='learning'}}
<div
  class='min-h-full bg-[#FAFAFA] py-8'
>
  <div class='max-w-6xl mx-auto px-6'>
    <div class='bg-white rounded-xl border border-[#E5E5E5] shadow-[0px_1px_2px_0px_rgba(0,0,0,0.05)] p-6'>
      <h2
        class='text-2xl sm:text-3xl font-bold text-gray-900 mb-8 text-center font-montserrat'
      >{{t 'test.logbook.editLogBook.logbookForm'}}</h2>

      <form
        x-data='{ loading: false }'
        @submit='loading = true'
        action='/logbooks/{{logbook.id}}?_method=PATCH'
        method='POST'
        enctype='multipart/form-data'
      >
        <div class='grid grid-cols-1 lg:grid-cols-2 gap-8'>

          <!-- Kolom Kiri -->
          <div class='space-y-6'>
            <!-- Activity -->
            <div>
              <label
                for="activity"
                class='block text-lg font-semibold text-[#404040] mb-3'
              >{{t 'test.logbook.editLogBook.activity'}}</label>
              <input
                type='text'
                id="activity"
                name="activity"
                required
                placeholder='{{t
                  "test.logbook.editLogBook.placeholderActivity"
                }}'
                value='{{logbook.activity}}'
                class='w-full h-[44px] px-[14px] bg-white border border-[#D4D4D4] rounded-[8px] font-inter text-base text-[#404040] placeholder:text-[#A3A3A3] focus:outline-none focus:border-[#003060] transition-colors'
              />
            </div>

            <!-- Activity Details -->
            <div>
              <label
                for="activityDetails"
                class='block text-lg font-semibold text-[#404040] mb-3'
              >{{t 'test.logbook.editLogBook.activityDetails'}}</label>
              <textarea
                name="activityDetails"
                id="activityDetails"
                required
                placeholder='{{t
                  "test.logbook.editLogBook.placeholderActivityDetails"
                }}'
                rows='6'
                class='w-full px-[14px] py-[10px] bg-white border border-[#D4D4D4] rounded-[8px] font-inter text-base text-[#404040] placeholder:text-[#A3A3A3] focus:outline-none focus:border-[#003060] transition-colors resize-none'
              >{{logbook.activityDetails}}</textarea>
            </div>
          </div>

          <!-- Kolom Kanan -->
          <div class='space-y-6'>
            <!-- Obstacle -->
            <div>
              <label
                for="obstacles"
                class='block text-lg font-semibold text-[#404040] mb-3'
              >{{t 'test.logbook.editLogBook.obstacles'}}</label>
              <input
                type='text'
                id="obstacles"
                name="obstacles"
                required
                placeholder='{{t
                  "test.logbook.editLogBook.placeholderObstacles"
                }}'
                value='{{logbook.obstacles}}'
                class='w-full h-[44px] px-[14px] bg-white border border-[#D4D4D4] rounded-[8px] font-inter text-base text-[#404040] placeholder:text-[#A3A3A3] focus:outline-none focus:border-[#003060] transition-colors'
              />
            </div>

            <!-- Photo Documentation -->
            <div
              x-data="{ previewUrl: '{{logbook.documentation}}' }"
              class='space-y-4'
            >
              <label
                for='documentation'
                class='block text-lg font-semibold text-[#404040] mb-3'
              >
                {{t 'test.logbook.editLogBook.photoDocumentation'}}
              </label>
              <input
                type='file'
                id='documentation'
                name='documentation'
                accept='image/*'
                x-ref='fileInput'
                class='w-full border border-[#D4D4D4] rounded-xl px-5 py-4 bg-white/70 hover:bg-white focus:outline-none focus:border-[#003060] focus:border-[#D4D4D4] transition-all duration-300 text-[#404040] file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-slate-100 file:text-[#404040] hover:file:bg-slate-200'
                @change="previewUrl = $event.target.files[0] ? URL.createObjectURL($event.target.files[0]) : '{{logbook.documentation}}'"
              />

              <!-- Image Preview -->
              <template x-if='previewUrl'>
                <div class='relative'>
                  <div
                    class='p-4 bg-[#FAFAFA] rounded-xl border border-[#E5E5E5]'
                  >
                    <img
                      :src='previewUrl'
                      alt='Photo Preview'
                      class='w-full max-w-xs h-48 object-cover rounded-lg border-2 border-[#D4D4D4] shadow-sm mx-auto'
                    />
                    <button
                      type='button'
                      class='absolute -top-2 -right-2 bg-red-500 text-white w-8 h-8 rounded-full flex items-center justify-center shadow-[0px_1px_2px_0px_rgba(0,0,0,0.05)] hover:bg-red-600 transition-all duration-300'
                      @click='previewUrl = null; $refs.fileInput.value = null;'
                    >
                      <svg
                        class='w-4 h-4'
                        fill='none'
                        stroke='currentColor'
                        viewBox='0 0 24 24'
                      >
                        <path
                          stroke-linecap='round'
                          stroke-linejoin='round'
                          stroke-width='2'
                          d='M6 18L18 6M6 6l12 12'
                        />
                      </svg>
                    </button>
                  </div>
                </div>
              </template>

              <!-- Current Image Info -->
              {{#if logbook.documentation}}
                <div
                  class='text-sm text-[#737373] bg-slate-50 px-4 py-2 rounded-lg border border-[#E5E5E5]'
                >
                  <p>{{t
                      'test.logbook.editLogBook.spanEditPhotoDocumentation'
                    }}</p>
                </div>
              {{/if}}
            </div>

            <div>
              <label
                for="otherDocumentation"
                class='block text-lg font-semibold text-[#404040] mb-3'
              >
                {{t 'test.logbook.createLogBook.linkDocumentation'}}
              </label>

              <input
                type='text'
                id="otherDocumentation"
                name="otherDocumentation"
                required
                placeholder='{{t
                  "test.logbook.editLogBook.placeholderLinkDocumentation"
                }}'
                value='{{logbook.otherDocumentation}}'
                class='w-full h-[44px] px-[14px] bg-white border border-[#D4D4D4] rounded-[8px] font-inter text-base text-[#404040] placeholder:text-[#A3A3A3] focus:outline-none focus:border-[#003060] transition-colors'
              />
            </div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div
          class='flex items-center gap-4 pt-10 mt-8 border-t border-[#E5E5E5]'
        >
          <button
            :disabled='loading'
            type='submit'
            class='inline-flex h-[44px] items-center justify-center gap-2 rounded-[8px] bg-[#003060] px-[18px] font-inter text-base font-semibold text-white transition-colors hover:bg-[#00254a] flex-1'
          >
            <span
              x-show='loading'
              class='loading loading-spinner loading-sm'
            ></span>
            <span x-show='!loading'>{{t
                'test.logbook.editLogBook.updateLogbook'
              }}</span>
          </button>
          <a
            href='/program/myProgram/{{user.id}}?courseId={{logbook.session.weeks.course.id}}'
            :disabled='loading'
            class='inline-flex h-[44px] items-center justify-center gap-2 rounded-[8px] border border-[#D4D4D4] bg-white px-[18px] font-inter text-base font-semibold text-[#003060] shadow-[0px_1px_2px_rgba(0,0,0,0.05)] transition-colors hover:bg-[#F5F5F5] flex-1'
          >
            {{t 'test.logbook.editLogBook.cancel'}}
          </a>
        </div>
      </form>
    </div>
  </div>
</div>
{{/user/shell_frame/index}}
